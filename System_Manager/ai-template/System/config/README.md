# System Config

Workspace-level app configuration that is not owned by a single view.

CLI/harness policy belongs here. View folders must not contain CLI/model/provider policy files.

Examples:

- `cli.json`
- future workspace-level model defaults
- future thinking effort defaults
