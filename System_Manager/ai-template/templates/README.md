# Templates

Template source area for views and workspace profiles.

- `view-templates/` stores canonical v2 view shells that ship with Fusion Studio.
- `workspace-templates/` stores workspace templates such as startup and new workspace profiles.
- `tools/`, `skills/`, and `scripts/` can define global and view-scoped assets for automatic installation.
