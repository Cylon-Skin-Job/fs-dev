---
id: RCC-0093
title: 'Secrets Manager — update docs from macOS Keychain to Electron safeStorage'
assignee: rccurtrightjr
created: 2026-05-24T02:30:00.000Z
author: rccurtrightjr
state: open
---

The Secrets Manager implementation has switched from macOS Keychain (`security` CLI) to Electron `safeStorage`. All documentation, specs, and wiki articles must be updated to reflect this.

### Files to update

1. `docs/SECRETS_MANAGER_SPEC.md` — §2d, §6b, §7a, §7d: replace Keychain references with safeStorage
2. `docs/CLIPBOARD_KEYCHAIN_REDESIGN.md` — title and content references
3. `AGENTS.md` — Secrets section (macOS Keychain → Electron safeStorage)
4. `ai/<machine>/Wiki/system-tools/secrets-manager/PAGE.md` — shell-capture pattern update
5. `fusion-studio-server/lib/secrets.js` — account string, backend comments

### What changes

| Before | After |
|--------|-------|
| `security find-generic-password -a "open-robin"` | Electron `safeStorage` API |
| macOS Keychain | Electron `safeStorage` (cross-platform) |
| `account = "open-robin"` | `account = "fusion-studio"` (or Electron app name) |

### Open questions

- [ ] What is the exact `safeStorage` API shape? (`encryptString` / `decryptString`?)
- [ ] Does the backend need a Node native addon, or can main process expose it via IPC?
- [ ] Is the `security` CLI fallback kept for dev-mode outside Electron?
