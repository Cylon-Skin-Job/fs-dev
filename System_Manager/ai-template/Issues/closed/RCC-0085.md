---
id: RCC-0085
title: 'Continue view architecture build — Chunk D next'
assignee: rccurtrightjr
created: 2026-05-22T00:00:00.000Z
author: rccurtrightjr
state: closed
completed: 2026-05-22T11:35:00.000Z
---

Session 3 handoff. Five chunks complete (A, A2, K, B, C). App boots cleanly. At the time, Chunk D was framed as an iframe loader. That direction is now superseded for built-ins: built-in product views render React, and the `app/index.html` path remains for non-built-in custom/local HTML views.

**Handoff doc:** `docs/handoffs/2026-05-22-view-architecture-session-3.md`

**Roadmap:** `ai/<machine>/Views/*-doc-viewer/specs/ROADMAP-REVISED.md`

### Historical Chunk D — What Needed to Happen

1. **Write the spec** — `ai/<machine>/Views/*-doc-viewer/specs/chunks/chunk-D-view-loader-iframe.md`
   - Current routing: built-in IDs render React components first
   - Custom/local HTML path: non-built-in views with `app/index.html` mount through `fusion-studio://`
   - Custom iframe instances use CSS toggle for active/inactive state when needed
   - Secure `postMessage` with `fusion-studio://` origin validation

2. **First code task** — Delete `fusion-studio-server/lib/watch/views-watcher.js`, replace with a `file:changed` UEB listener that filters for `ai/<machine>/Views/` paths. No view ever gets its own backend watcher.

3. **Then** — preserve React built-ins while adding the custom/local HTML iframe path.

### Chunk E follows Chunk D

Theme token bridge — shell reads `ai/<machine>/System/styles/themes.json`, broadcasts computed CSS tokens to active custom/local iframe views via `postMessage`.

### Session Start Checklist

- [x] `git log` — see what background sessions committed
- [x] Check `ai/<machine>/Issues/inbox/` for any new tickets
- [x] Read `docs/handoffs/2026-05-22-view-architecture-session-3.md` for full context
- [x] Read `ai/<machine>/Views/*-doc-viewer/specs/ROADMAP-REVISED.md` Chunk Spec Index for current status

### Implementation Log

- Deleted `fusion-studio-server/lib/watch/views-watcher.js`
- Removed `views-watcher` import and startup call from `lib/startup.js`
- Added `hasAppHtml?: boolean` to `PanelConfig` type in `lib/panels.ts`
- Added `app/index.html` probe in `loadPanelConfig` (parallel with existing `ui/module.js` probe)
- Rewrote `ContentArea.tsx` with guarded routing: built-in React component map first; non-built-in `config.hasAppHtml` → `<iframe src="fusion-studio://{panel}/app/index.html">`
- Added `.rv-view-iframe` CSS rule in `App.css`
- Client builds cleanly (`npm run build` passes, `tsc --noEmit` clean)
- Server syntax check passes (`node --check lib/startup.js`)
- No existing views have `app/index.html`, so all panels fall through to React components — zero disruption
