Optional view template bundles.

Each child folder is an installable bundle. Copy or merge the bundle contents into a workspace root, route `ai/issues/.templates/**/TICKET.md` through ticket-id generation, and copy `.claude/skills/**/SKILL.md` into the project-local skill path.
