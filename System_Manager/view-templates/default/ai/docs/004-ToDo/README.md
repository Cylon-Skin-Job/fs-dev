# To-Do Docs

Use this folder for small actionable items that are somewhat decided but do not need a full spec.

If a to-do grows into scoped implementation work with dependencies, pass/fail conditions, or handoff needs, move it toward a spec or ticket.
