Workspace tool definitions installed by the default template.

Tools are copied into the workspace but should still be gated by the System Manager approval/policy model before protected mutations run.
