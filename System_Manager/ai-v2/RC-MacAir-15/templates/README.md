# Templates

Template source area for views and workspace profiles.

- `views/` stores default view shells that ship with Fusion Studio.
- `workspaces/` stores workspace templates such as startup and new workspace profiles.
- `tools/`, `skills/`, and `scripts/` can define global and view-scoped assets for automatic installation.
