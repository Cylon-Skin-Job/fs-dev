# Workspace-db

Optional repo-local mirror database storage.

`workspace.db` should be created here only after workspace DB mirroring is enabled. The protected Electron SQLite database remains canonical infrastructure.
