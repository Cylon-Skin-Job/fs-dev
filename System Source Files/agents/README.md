# Agent Templates

Agent definitions that ship with Fusion Studio.

Place agent folders here to make them available as templates when users create or configure AI agents in their workspace.

Each agent template should include:
- `index.json` — agent identity, capabilities, and default prompt
- `prompt.md` — default system prompt
- `tools/` — available tool definitions (optional)
