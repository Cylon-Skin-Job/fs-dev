---
id: RCC-0085
title: 'Continue view architecture build — Chunk D next'
assignee: rccurtrightjr
created: 2026-05-22T00:00:00.000Z
author: rccurtrightjr
state: closed
completed: 2026-05-22T11:35:00.000Z
---

Session 3 handoff. Five chunks complete (A, A2, K, B, C). App boots cleanly. Next up is Chunk D — the iframe view loader that replaces the static `ContentArea.tsx` panel map.

**Handoff doc:** `docs/handoffs/2026-05-22-view-architecture-session-3.md`

**Roadmap:** `ai/views/doc-viewer/specs/ROADMAP-REVISED.md`

### Chunk D — What Needs to Happen

1. **Write the spec** — `ai/views/doc-viewer/specs/chunks/chunk-D-view-loader-iframe.md`
   - Dual-track: `ContentArea.tsx` checks for `app/index.html` → mount iframe; else fall through to existing React component
   - Warm iframes: all created on workspace load, CSS toggle for active/inactive
   - `PanelWrapper` component hosts the iframe
   - Secure `postMessage` with `fusion-studio://` origin validation

2. **First code task** — Delete `fusion-studio-server/lib/watch/views-watcher.js`, replace with a `file:changed` UEB listener that filters for `ai/views/` paths. No view ever gets its own backend watcher.

3. **Then** — implement the dual-track `ContentArea.tsx` changes per the spec.

### Chunk E follows Chunk D

Theme token bridge — shell reads `ai/system/styles/themes.json`, broadcasts computed CSS tokens to active view iframe via `postMessage`.

### Session Start Checklist

- [x] `git log` — see what background sessions committed
- [x] Check `ai/views/issues-viewer/inbox/` for any new tickets
- [x] Read `docs/handoffs/2026-05-22-view-architecture-session-3.md` for full context
- [x] Read `ai/views/doc-viewer/specs/ROADMAP-REVISED.md` Chunk Spec Index for current status

### Implementation Log

- Deleted `fusion-studio-server/lib/watch/views-watcher.js`
- Removed `views-watcher` import and startup call from `lib/startup.js`
- Added `hasAppHtml?: boolean` to `PanelConfig` type in `lib/panels.ts`
- Added `app/index.html` probe in `loadPanelConfig` (parallel with existing `ui/module.js` probe)
- Rewrote `ContentArea.tsx` with dual-track logic: `config.hasAppHtml` → `<iframe src="fusion-studio://{panel}/app/index.html">`; else existing static component map
- Added `.rv-view-iframe` CSS rule in `App.css`
- Client builds cleanly (`npm run build` passes, `tsc --noEmit` clean)
- Server syntax check passes (`node --check lib/startup.js`)
- No existing views have `app/index.html`, so all panels fall through to React components — zero disruption
