# Chat Composition Roadmap Issues

> Material gates, reconciled contradictions, and non-blocking deferrals for the
> exact roadmap candidate.

| ID | Issue | Authority | Status | Resolution or gate |
|---|---|---|---|---|
| CHAT-I-001 | Agent Tool Provenance is approved for implementation but not yet owner-accepted. | owner/external SPEC | open external gate | SPEC-01 cannot dispatch until final owner acceptance and accepted changed-path evidence exist. |
| CHAT-I-002 | Generic Component Tab Host is only partially implemented in the current worktree. | active code/external SPEC | open external gate | SPEC-02 cannot dispatch until all host slices, tests, final review, commit, and owner acceptance are complete. |
| CHAT-I-003 | Main currently ends at migration 034 while the Provenance worktree introduces another 034 plus 035/036. | active code constraint | resolved for planning | SPEC-01 uses the next free migration after the accepted/rebased prerequisite. It never hardcodes or edits a migration filename before that baseline exists. |
| CHAT-I-004 | Earlier scope coupled stable view-ID migration to later System relocation, creating a circular prerequisite. | owner/source contract | validated | SPEC-01 performs only the registry-owned missing-ID preflight before group backfill. Relocation stays in the System View domain and preserves those IDs. Invalid/duplicate/unwritable capsules block preflight instead of silently downgrading their threads. |
| CHAT-I-005 | A schema-only release could leave sessions created between schema migration and protocol cutover ungrouped. | migration gap | validated | SPEC-01 does not publicly activate group-backed lists until creation, Fork removal, final reconciliation, and every session-producing path are group-safe in the same SPEC. |
| CHAT-I-006 | Current thread creation and deletion pair SQLite rows with Markdown mirrors without crash-complete recovery. | active code constraint | validated | SPEC-01 keeps ThreadManager as owner and adds transaction-capable creation plus durable creation/deletion recovery before group activation. |
| CHAT-I-007 | `current_primary_thread_id` and membership form a cyclic integrity constraint. | schema constraint | validated | Use deferred relational enforcement or an equivalent transaction/repository invariant that cannot commit a nonempty group without exactly one valid current member. The builder must prove the selected mechanism. |
| CHAT-I-008 | Current global renderer state can cross usage, readiness, model selection, menu state, and open responses between mounted chats. | active code constraint | validated | SPEC-02 moves session facts to `threadId`, mounted facts to `surfaceId`, and population selection to `{workspaceId, viewId}` before exposing reusable surfaces. |
| CHAT-I-009 | Worksurface persistence has a different owner and recovery model from renderer chat composition. | owner scoping rule | validated | It is isolated as SPEC-03 instead of being hidden inside the chat component extraction. |
| CHAT-I-010 | Move changes SQLite structure and file-backed view state in one user action. | persistence standard | validated | SPEC-04 commits the group transition first and uses SPEC-03's service-owned managed-placement lane and durable outbox; delivery failure remains retryable and never rolls back the primary transition. |
| CHAT-I-011 | Current floating/sticky Secondary Chat is a singleton and cannot serve as the reusable Side Chat foundation. | active code constraint | validated | SPEC-04 removes it only after the component-tab replacement passes; no compatibility alias remains. |
| CHAT-I-012 | Pending New Chat is a separate creation-lifecycle domain and its current draft SPEC requires independent reconciliation with accepted Provenance. | later owner scope decision | deferred | The owner narrowed this roadmap to current eager creation plus group atomicity. Schedule Pending New Chat after SPEC-01 as a separate roadmap/SPEC; CHAT-RD-008 records the narrow supersession of D-168's older packaging rule. |
| CHAT-I-013 | Authority-bearing commands currently cannot distinguish the Fusion shell from a raw local WebSocket client. | routed standard/active code | validated | SPEC-01 must establish and test a narrow server-verified shell connection role before public activation. Broader protected-System permissions and general remote authentication remain separate. |
| CHAT-I-014 | Collections, System capsule relocation, automatic naming, transcript export, Send to Chat expansion, and project/CWD creation are separate domains. | owner scoping rule | deferred | They remain explicit downstream roadmaps and cannot enter an implementation deviation for these SPECs. |
| CHAT-I-015 | The Generic Component Tab Host intentionally provides no placement, target matching, dedupe, or persistence command. | external prerequisite contract | validated | SPEC-04 owns a code-level Side Chat placement coordinator and bridge over SPEC-03 view state; the Generic Host only validates/resolves/renders descriptors. |
| CHAT-I-016 | Adapter content writes could erase Side Chat descriptors or close dispositions. | persistence/state standards | validated | SPEC-03 separates adapter-owned content and service-owned managed placements with independent revisions and server-side lane-preserving merge rules. |
| CHAT-I-017 | Switching before outgoing worksurface acknowledgement could lose the latest capture on conflict/process loss. | source contract | validated | SPEC-03 keeps the outgoing surface selected/mounted until success; conflict, rejection, or timeout requires retry/reconcile or explicit warned discard. |
| CHAT-I-018 | A closed Side Chat member would be undiscoverable after restart without a group-member protocol. | acceptance gap | validated | SPEC-04 defines qualified `thread:members` hydration and idempotent `open_member_in_side`; SPEC-01's count is numeric and initially returns one. |
| CHAT-I-019 | The launch master must not be exposed through renderer/preload JavaScript. | trusted-shell contract | validated | SPEC-01 uses a one-read bootstrap pipe plus main-owned one-use HMAC proof; renderer receives only runtime descriptor, challenge, and proof. |
| CHAT-I-020 | Existing `activity.tabs` and related top-level writers could become a second worksurface owner. | state standard/active code | validated | SPEC-03 inventories and cuts over every overlapping writer/hydrator while group-bound and proves the group content lane is sole owner. |
| CHAT-I-021 | Move/Delete ordering was ambiguous. | persistence standard | validated | One group lease serializes both: Move-first completes then Delete removes both members/placements; Delete-first rejects Move without creation. |
| CHAT-I-022 | Adapterless chat-enabled views had no production Side Chat host. | product/source contract | validated | SPEC-04 supplies a code-owned bridge for native adapters and runtime-only roots for Issues, Wiki, Browser, Agents, and other eligible adapterless views. |
| CHAT-I-023 | Move activity conflicted with the bundle MRU rule. | product/source contract | validated | Accepted Move writes `move:{requestId}`/`move-chat-to-side` and advances MRU once; retry, close, repair, and reopen do not. |
| CHAT-I-024 | Earlier slice packets were horizontal implementation phases. | testing standard | validated | All four SPECs now require public-route vertical increments crossing owner, persistence/state, renderer, fan-out, and restart/readback before clean slice return. |
| CHAT-I-025 | A proof API on the same localhost origin as workspace HTML could be delegated through `parent.electronAPI`. | trusted-shell/source contract | validated | SPEC-01 moves the shell to an Electron-owned origin, separates content origins, enforces main/subframe navigation plus CSP/CORS policy, and tests parent/preload delegation failure before enabling proofs. |
| CHAT-I-026 | Move could not deterministically inherit a model/variant when the renderer held an unacknowledged or panel-global choice. | session-authority/source contract | validated | SPEC-01 implements standards-defined `set_harness_selection` by exact `threadId`; SPEC-02 renders pending/acknowledged state; SPEC-04 reads only the last acknowledged server value. |
| CHAT-I-027 | The local member menu did not preserve the source contract for exact-member application links. | source contract | validated | SPEC-04 extends version-1 group links with validated `threadId`; resolution reopens/focuses the lifetime Side Chat placement without promotion or MRU activity. |
| CHAT-I-028 | Primary-event reason vocabulary differed between foundation and Move. | schema contract | validated | Both SPECs use the single durable value `move-to-side-chat`. |
| CHAT-I-029 | SPEC-01 and SPEC-04 both appeared to own non-primary exact-member application-link behavior. | scope/dependency contract | validated | SPEC-01 supports the standard optional sole/current member but opens Main Chat; SPEC-04 exclusively adds non-primary link production and Side Chat reopen/focus once multi-member placement exists. |
| CHAT-I-030 | Delete with a new request ID could lose access to retained cleanup recovery. | deletion-recovery contract | validated | While the bounded tombstone remains, any new-ID Delete must return the retained aggregate and resume idempotent repair; focused restart coverage is required. |
| CHAT-I-031 | `set_harness_selection` wording could let the client change server-owned harness identity. | WebSocket standard | validated | The action accepts only portable `{model, variant}`; harness binding is read from immutable server session state and carried to Move without client authority. |
| CHAT-I-032 | A recovered Delete aggregate was not durably bound to the new request ID. | action-idempotency contract | validated | Tombstone recovery records the aggregate as the new invocation's action result, so replay remains valid after tombstone expiry. |
| CHAT-I-033 | Move did not assign the required membership origin kind. | membership schema/source contract | validated | The new Main member is stored with `origin_kind='move-to-side-chat-primary'`. |
