# RCC-0108 SPEC-04 Orchestrator Ledger — Client Routing and Frontier Restoration

**Orchestrator:** spec-slice-builder/orchestrator run 1 for SPEC-04 (this document)
**Date opened:** 2026-08-26
**Worktree:** `/Users/rccurtrightjr./projects/fs-dev-rcc-0108` · branch `agent/rcc-0108-roadmap` · HEAD `4f972c5` · nothing committed
**Isolation contract:** preserved. 56 dirty porcelain entries arrive accepted as SPEC-01/02/03 uncommitted bytes + docs. Zero client files dirty at open — SPEC-04 is the first client-touching SPEC on this branch. Only SPEC-04-authored changes are attributed here.

This ledger is append-only. States: `pending → implementing → builder_review → acceptance_review → ACCEPTED | BLOCKED`.

---

## 1. Authority stack applied (highest first)

1. Supervisor packet (2026-08-26 delegation of SPEC-04 run 1) + explicit user direction in that packet.
2. Owner decisions R1–R10, all closed (`RCC-0108-implementation-readiness-issues.md`; queue closed 2026-07-22).
3. Parent product contract §§4–8 (`RCC-0108-chat-working-step-activity.md`) — authoritative product/technical contract.
4. Roadmap §5.3 (client frontier algorithm — binding core), §5.7 (one job per file; new/extracted ≤400L; `LiveSegmentRenderer.tsx` DO-NOT-SPLIT exception), §5.5 sentence 2 (client WS logger suppression), §7 (verification ownership: client route/turn gate + helper isolation + snapshot/live race owned here; every Playwright command `--workers=1`), §10 (coordination boundary).
5. SPEC-04 text (`RCC-0108-SPEC-04-client-routing-frontier.md`) — outcome, algorithm, slices, expected surface, §6 exact gate, §7 acceptance criteria, §8 handoff.
6. Accepted prerequisites as amended:
   - SPEC-01 runtime drain ownership (owner-accepted 2026-08-25; `applyLiveMutation` primitive added by amendment).
   - SPEC-02 server step frontier (owner-accepted 2026-08-25) — report §9 wire union + `LiveTurnSnapshot` shape are the consumed contract; banked advisories: tolerate sequence-less legacy emissions (decision #11), never read usage from terminal snapshots (S2-D9), wire omissions reconstructible from the snapshot mirror.
   - SPEC-03 terminal errors & diagnostics (owner-accepted 2026-08-26) — report §7 obligations are BINDING downstream packet impacts on this SPEC (frozen retrieval wire shapes incl. null-echo convention; envelope absent-key semantics; mandatory client-side diagnostic log suppression end-to-end; retrieval discipline; line-boundary audit list carry-forwards).
7. Chat Wiki overview + Testing/Operations browser-Playwright page; Code Standards front page, WebSocket Protocol standard, State Management standard; AGENTS.md (active directories only; smallest correct change; preserve unrelated dirty work).

## 2. Preflight evidence (recorded)

| Check | Result |
|---|---|
| SPEC approval | SPEC-03 `accepted` with explicit owner receipt in supervisor ledger; SPEC-04 delegated ("spawning run 1") |
| Worktree/branch | `agent/rcc-0108-roadmap`, HEAD `4f972c5`, nothing committed |
| Dirty-entry attribution | 50 server + docs entries = accepted SPEC-01/02/03 bytes; **0 fusion-studio-client entries** |
| Client dependencies | were absent → `npm install` in `fusion-studio-client/` completed this preflight (audit advisories only, as before) |
| Playwright | 1.58.2; chromium-1208 + headless shell cached |
| Client production build baseline (pre-change) | `npm run build` ✓ built in 4.15s; chunk-size warning is pre-existing advisory noise |
| Stale-symbol baseline | `stepNumber` ×1 at `src/types/index.ts:454` (removal owned by Slice A); `StepBegin` literal ×0; `streamRevision` ×0 |
| Server arrival reference (from packet; not re-run at open) | 89 suites / 1302 passed / 1 pre-existing skip / 0 failed |

## 3. Slice plan

Dependency order strictly serial (B needs A's types; C needs B's routed helpers; D proves A–C through the browser fixture). One slice writer active at a time.

### Slice A — Types and state boundaries
Scope: split oversized `src/types/index.ts` (793L) into focused `chat.ts` / `websocket.ts` / `workspace.ts` / `view-state.ts` behind an index re-export hub (compatibility preserved for every existing importer); add parent activity/cursor/seen-ledger/activityRevision/terminalError/diagnostic/live-snapshot shapes (typed routing/frontier transport only — SPEC-05 owns observable transition proof); add required positive-integer `streamSeq` to every in-flight wire family member; `status_update` gains required `turnId`+`streamSeq`; delete `stepNumber` + any legacy `StepBegin` shapes (`step_begin` canonical type stays); add frontier state/actions keyed `threadId+turnId` without pushing a multi-job file past 400L; freeze upstream shapes exactly as specified (absent-key envelope semantics; snapshot always carries `terminalError` field, null-init).
State: **ACCEPTED** — rev pinned by SHA-256 in builder handoff §9 (index.ts ca637e26…, chat.ts 9ed8b1ac…, chat-wire.ts 74f48a4d…, websocket.ts a44dd3fb…, workspace.ts 5d8c2031…, view-state.ts 39f6330f…, frontier.ts dae2124c…). Gate chain: builder self-review+repairs (5 findings, all fixed same-session) → builder-owned reviewer CLEAN 1st pass (#2) → orchestrator independent inspection + reruns green → acceptance reviewer CLEAN 1st pass (#3), 0 material, advisories A1–A5 recorded below.

### Slice B — Routed handler modules
Scope: thin routed dispatcher in `stream-handlers.ts`; extract focused tool/registry-frontier/diagnostic handler modules; explicit turn gates (turn_begin null/same/different-ID rules; every later in-flight message matches addressed current turn BEFORE any store/helper mutation; wrong-thread/wrong-turn = drop-before-mutation); sequence requirement + gap-safe buffering entry points (buffer/drain machinery per §5.3 consuming Slice C); key tool-arg buffers/grouping/subagent state by thread+turn; initialize/reset/finalize only the addressed namespace; `step_begin` never resets tool/subagent state; step_begin routed typed (transport) with route validation, no observable Working mutation; post-terminal saved/metadata correlation rules preserved and exempt from live-current-turn gate; diagnostic response handling + MANDATORY log suppression in `redactMessageForLog()` before console/captured-log paths; tolerance policy for sequence-less disabled-legacy shapes = safe diagnose-and-drop, never crash/frontier corruption (decision #11).
State: **ACCEPTED** — rev SHA-256 pinned in builder handoff §12 (9 paths: stream-handlers 418L, turn-lifecycle 229L, subagent-stream 180L, tool-grouper 227L, ws-client 412L, websocket.ts 187L, stream-helper-registry NEW 224L, tool-stream-handlers NEW 252L, chat-diagnostic-handlers NEW 196L). Gate chain: builder self-review caught 5 findings incl. one critical-class design bug (drain-vs-live-gate baseline conflict; repaired via take→apply protocol) → builder-owned reviewer CLEAN 1st pass (#5; 0 material; advisories A1–A5) → orchestrator inspection + independent reruns green (build ✓ 3.50s / tsc OK / eslint OK / sweep exit 1 / dirty 64→72 = exactly 8 slice entries) → acceptance reviewer CLEAN 1st pass (#6) with independent 42/42 behavioral proof on real bundled modules + captured-console zero-leak sweep. Residual risks R1–R5 recorded in report.

### Slice C — Snapshot and save correlation
Scope: exact §5.3 frontier algorithm (atomic already-revealed baseline install at N; drop ≤N; buffer >N by sequence; contiguous exactly-once drain from N+1; gaps never guessed; newer snapshot advances through buffered gaps; cross-thread isolation); completed/durable-or-terminal snapshots through instant completed-message path; in-flight segments installed as already revealed; terminal-error envelope retained onto completed-message state when durable history lacks the matching exchange; later save ack merges without duplicate message/content; hydrating A cannot reset or delay B.
State: **ACCEPTED** — final rev SHA-256 pinned in repair handoff §3 (snapshot-restore.ts cca59e1b… @400L after repair; other five files byte-identical to reviewed candidates ae830491/c8225ad8/0a7aeba7/6fada374/f6e07346/e2bd8b19 lineage). Files: NEW live-route.ts (208L), NEW snapshot-restore.ts (400L); MODIFIED stream-handlers.ts (415L), stream-helper-registry.ts (330L), thread-handlers.ts (269L), chatSlice.ts (367L). Gate chain: builder self-review caught 4 real defects pre-spawn incl. multi-event drain splice loss → builder-owned reviewer CLEAN 1st pass (#7) → orchestrator inspection + reruns green → **acceptance review FINDINGS: 1 material F-C1** (turn-blind content leg in slotRepresentsTurn could strand a wiped slot when a prompt-twin exchange exists) → consolidated repair routed to responsible builder → id-deterministic detector fix + FC1 probe/control regression pair (smoke 88/88) → fresh builder-side reviewer CLEAN on repaired bytes 1st pass (#9) → orchestrator re-verification green (tsc/eslint/sweep; porcelain stable at 76). First clean pass on repaired bytes stops the loop.

### Slice D — Deterministic browser fixture and protocol proof
Scope: `e2e/working-activity.spec.ts` + deterministic WebSocket routing/proxy fixture (no product-visible hooks; follows existing e2e helper convention); `@routing` + `@frontier` cases ONLY (no @working/@terminal-error — those belong to SPEC-05); drive explicit server-shaped frames with real threadId/turnId/streamSeq/activityRevision; prove state through public UI/store-observable packaged-client behavior (no new production test API); negative/no-replay/gap/duplicate/cross-thread cases covered publicly (S4-C-D6 notes module-boundary evidence was accepted for case 15); every run `--workers=1`.
State: **ACCEPTED** — run 3, 2026-08-27. Final revision identity SHA-256 short: spec `working-activity.spec.ts` 6ce29959 @400L · support/scenario.ts 06e53ae5 @214L · support/wire.ts 87f952dd @287L · support/ws-fixture.ts 4add026f @349L (total 1250L). Gate chain: fresh builder #11 READY_FOR_ORCHESTRATOR_REVIEW (22-test lane green after D-D3..D8/D10 repairs confined to owned bytes) → builder-owned reviewer #12 CLEAN first pass → orchestrator independent inspection + full verbatim §6 rerun green (eslint exit 0 / build ✓3.55s / lane **22 passed 25.8s** / sweeps clean / continuity lane 1 passed; server suite not re-run — zero server bytes in SPEC, arrival reference stands) → acceptance reviewer #13 CLEAN first pass (independently re-ran lane 22 passed 26.1s + sweeps + production-byte verifications; redaction seam verified at src/lib/ws-client.ts:120 — actual path footnote vs briefing's ws/) — gate stopped at first clean pass.
- Run-2 resumed 2026-08-27; run 1 owner-cancelled after suspected stuck subagent (infrastructure failure, never a review failure; no child ever registered for D in runs 1–2). Run 3 spawned fresh builder #11.
- Frozen candidate owned (run-1 scaffold tail): spec 400L/wire 286L/scenario 213L/fixture 289L; `${PANEL}` scenario defect found ALREADY fixed in current bytes; probe correctly absent (S4-C-D6 throwaway cleanup — deletion formalized as S4-D-D1).
- Builder #11 handoff: **READY_FOR_ORCHESTRATOR_REVIEW** — run-history: initial gate 15/22 → systematic root-causing of lane degradation → repairs confined to D-owned bytes: (1) view-state neutrality shim D-D3 after proving pre-shim boots wrote runtime state into owner `ai/**` files and later boots froze on poisoned persisted state (reproduced against legacy specs before fix, gone after); (2) per-thread `openHandled` settle barriers D-D4 replacing absolute settled arithmetic (observed hang served=4<min=5); (3) server-shaped `contextUsage` fraction frames D-D5 (translator emits none of `tokenUsage.context_pct`; client projects top-level fraction); (4) `.streaming` expectations corrected to §5-exact guarantees D-D6; (5) A/B isolation case redesigned through canonical-mirror reopen honoring slot-wipe=replacement semantics (S4-C-D6 lineage) D-D7; (6) `subagentEventType:'TurnBegin'` production vocabulary D-D8. Spec held at exactly 400L throughout via in-file compensation D-D10. No A/B/C/src/server byte touched.
- Builder-owned reviewer #12 CLEAN first pass (0 critical/high/material; advisories A residue-disposition/A' byte-exact console-format coupling cost only/C carry D-D3 masking residual into SPEC-05 packet).
- Orchestrator independent inspection (current bytes): verified hashes 6ce29959/06e53ae5/87f952dd/4add026f @400/214/287/349L; full coverage mapping vs every §5 Routing+Frontier bullet incl. suppression allowlist proof byte-exact end-to-end via `redactMessageForLog` seam + retrieval-discipline definition-only complement. Reran ALL §6 gates verbatim green: eslint 15-path exit 0 · build ✓3.55s · Playwright grep `--workers=1` **22 passed (25.8s)** · stale-symbol sweep src+E2E clean · continuity `thread-bootstrap-order.spec.ts --workers=1` 1 passed. Server suite NOT re-run — zero server bytes authored any SPEC-04 run (arrival reference 89/1302/1skip/0 stands).
- Residue investigation (advisory A closure): root-caused to pre-shim run-1-era fixture boots writing view-state through real dev-server persistence (`state:set` → `writeViewStatePatch`). Empirical containment test on current bytes: one Playwright case run flipped mtime but SHA identical ⇒ shim blocks ALL new value flow (rewrite-with-stale-disk only). Remediation executed by orchestrator (product bytes untouched): RC-MacAir-15 System/state.json single fixture-authored line restored to HEAD value (file now diffs EMPTY vs HEAD); untracked `ai/RCs-Air-2/System/state/state.json` (hostname-default machine identity) neutralized to `currentThreadId:null` so subsequent boot rewrites stay clean; Views/002-file-viewer/state.json cosmetic tab churn left untouched (no fixture markers; mixed attribution) — recorded as S4-D-D11/D12. Residue sweep of ai/** JSON for wa-* markers: clean.

## 4. Binding obligations carried into every slice packet

From SPEC-03 §7 (frozen, do-not-loosen):
1. Retrieval wire shapes: request `{type:'chat-turn:diagnostic:get', threadId, turnId, diagnosticId, workspaceId?}`; available `{type:'chat-turn:diagnostic:report', threadId, turnId, diagnosticId, report ≤24KiB}`; unavailable `{type:'chat-turn:diagnostic:unavailable', threadId?, turnId?, diagnosticId?}` ONE fixed value-free frame for every denial class; echoed identifiers may be `null` (client codes to null-echo convention).
2. Envelope availability: `TurnTerminalError {kind, code, message=exact catalog text, recoverable:true, diagnosticId?}` identical across wire turn_end / terminal snapshot / persisted `exchange.metadata.terminalError` / `chat-turn:saved` merge. `diagnosticId` PRESENT only on genuine persistence success, OMITTED otherwise, NEVER null. Absent-key = no-envelope: wire/persisted metadata OMIT `terminalError` on non-error terminals; snapshots ALWAYS carry the field (null-init).
3. MANDATORY client-side suppression of EVERY retrieved diagnostic report field before console logging or captured-log forwarding; only message type, fixed unavailable marker, opaque route/diagnostic identifiers may ever be logged. SPEC-04 owns this end-to-end (currently entirely unimplemented client-side).
4. Retrieval discipline: never auto-request because an error row mounted; retrieval actions only with valid `diagnosticId` (UI itself = SPEC-05).

Banked advisories honored: decision #11 sequence-less legacy `chat:status_update`/`chat:turn_end` tolerated safely; never read usage from terminal snapshots (S2-D9); wire omissions reconstructible from snapshot mirror (status_update omits messageId/planMode; turn_begin omits attachments).

Standing bans: no `streamRevision`; no second whole-turn counter; `streamSeq` remains the sole authoritative whole-turn order (equal/lower/greater `activityRevision` must never reorder or suppress non-activity output); no retrofitting of `LiveSegmentRenderer.tsx` internals (documented DO-NOT-SPLIT).

## 5. Child subagent registry (append-only; row for EVERY spawn)

| # | Role | Agent | Task ID | Spawned | Terminal result |
|---|---|---|---|---|---|
| 1 | Slice A builder | spec-slice-builder | ses_fbedbb7f9ffeZUeqp0UAdxxFUg | run 1 | READY_FOR_ORCHESTRATOR_REVIEW (terminal; 1 writer, no conflicts) |
| 2 | Slice A builder-owned review | clean-room-reviewer (spawned by builder #1) | ses_fbec47d4fffeQqBJ7spa1RUND6 | run 1 | CLEAN first pass — 0 critical/high/material; 4 advisories (terminal) |
| 3 | Slice A orchestrator acceptance review | clean-room-reviewer | ses_fbeb56798ffeeULmnsIMUy3UXP | run 1 | CLEAN first pass — 0 material; deviations D1–D9 classified accepted; advisories A1–A5 (terminal) |
| 4 | Slice B builder | spec-slice-builder | ses_fbea9a191ffecyPZvp6w0CoJTL | run 1 | READY_FOR_ORCHESTRATOR_REVIEW (terminal; self-review repairs incl. pre-gate critical design bug S3) |
| 5 | Slice B builder-owned review | clean-room-reviewer (spawned by builder #4) | ses_fbe8e72feffeLRh1vuGn5pR2yG | run 1 | CLEAN first pass — 0 critical/high/material; 5 advisories (terminal) |
| 6 | Slice B orchestrator acceptance review | clean-room-reviewer | ses_fbe804cebffe7XdmX1jR5hb52D | run 1 | CLEAN first pass — 0 material; D-B1..B6 compliant; 42/42 behavioral proof (terminal) |
| 7 | Slice C builder | spec-slice-builder | ses_fbe6e726bffej7F3tYLnWDHeTV | run 1 (+repaired same session) | READY_FOR_ORCHESTRATOR_REVIEW ×2 (original + F-C1 repair pass; both terminal) |
| 8 | Slice C builder-owned review | clean-room-reviewer (spawned by builder #7) | ses_fbe41bdbeffeef7JfpkIMMQmNF | run 1 | CLEAN first pass — 0 material; 3 advisories (terminal) |
| 9 | Slice C orchestrator acceptance review | clean-room-reviewer | ses_fbe32a954ffeLGfRjhOFZQIZ8P | run 1 | FINDINGS — 1 material (F-C1), 37/37 own smoke green; D1–D6 adjudicated (terminal FINDINGS → repaired) |
| 10 | Slice C repair-gate review (fresh, repaired bytes only) | clean-room-reviewer (spawned by builder #7 in repair cycle) | ses_fbe194191ffeVaZmUpwSWmwSwH | run 2 | CLEAN first pass on repaired bytes — 0 material; advisories A1/A2 carried (terminal; stops gate per first-clean rule) |
| 11 | Slice D builder | spec-slice-builder | ses_fbcef06e0ffei5F3r837CKehQP | run 3 | READY_FOR_ORCHESTRATOR_REVIEW (terminal; repairs D-D3..D8/D-D10 confined to owned bytes; spawn rejections none; one writer, no conflicts) |
| 12 | Slice D builder-owned review | clean-room-reviewer (spawned by builder #11) | ses_fbcb46888ffeEiZZMgd6QCTWGW | run 3 | CLEAN first pass — 0 critical/high/material; advisories A (residue disposition→resolved S4-D-D11/D12), A' (byte-exact console coupling maintenance cost), C (D-D3 masking residual → SPEC-05 packet) (terminal) |
| 13 | Slice D orchestrator acceptance review | clean-room-reviewer | ses_fbc8b8adaffeYRP0lGx96YP9uI | run 3 | CLEAN first pass — 0 material; all deviations D-D1..D10 adjudicated accepted; advisories: browser-lane retrieval-discipline negative proof indirect (SPEC-05 may add one-line sentFrames guard), redundant exports TURN_A2/threadListFrame style-tier only (terminal; stops gate per first-clean rule) |
| 14 | SPEC-04 final integration review | clean-room-reviewer | ses_fbad4349cffeWWFU2XSI1NzvKh | run 3 | CLEAN first pass — 0 material; cross-slice seams verified drift-free (A→B→C→D); integrated §7 criteria 1–8 evidence confirmed; all 33 deviations cross-checked nothing silently dropped; downstream impact for SPEC-05 = compatible deviations, none requiring correction, no owner ruling; independently re-ran lane 22 passed (26.2s) + sweeps + bounds audit (terminal) |

Lifecycle notes: every spawn terminated normally; no missing closure; no spawn rejections; single active slice writer at all times; each gate stopped at first clean pass. All descendants inherited the root thread's model/reasoning effort; none pinned or substituted.

## 6. Deviation ledger (append-only)

Slice A (builder-proposed D1–D9; orchestrator-final classifications confirmed by acceptance reviewer):

| ID | Original contract | Actual change | Reason | Files | Tests/checks | Observable effect | Risk | Downstream impact | Classification |
|---|---|---|---|---|---|---|---|---|---|
| S4-A-D1 | Expected surface names 4 split type modules | Fifth module `src/types/chat-wire.ts` holds routed required-shape contracts | Combined `websocket.ts` measured >400L, violating roadmap §5.7 new-file bound; two one-job wire surfaces stay bounded | chat-wire.ts, websocket.ts, index.ts | eslint/tsc/build green; export parity probe | None (type-only; hub API identical) | none | Slices B/C + SPEC-05 import routed shapes from hub | accepted |
| S4-A-D2 | Placement judged by consumers | PanelId/TIMING/ThemeEntry→view-state.ts; Thread*/HarnessStatus→workspace.ts | Packet list + domain cohesion | view-state.ts, workspace.ts | build+tsc | none | none | none | accepted |
| S4-A-D3 | Optional panelStore/chatSlice placeholder wiring authorized | Not done | Smallest correct change; Slice B wires dispatch where state becomes live | (none) | n/a | none | none | B initializes frontiers where routing lands | accepted_no_downstream_impact |
| S4-A-D4 | usage `{contextUsage?,tokenUsage?,messageId?,planMode?}\|null` | Members nullable-tolerant (`number\|null` etc.) | Server mirror serializes explicit nulls (live-turn-snapshot.js); transport truth | chat.ts | reviewer server-bytes check | none | none | consumers treat null≡undefined; never read usage from terminal snapshots (S2-D9 comment carried) | accepted_no_downstream_impact |
| S4-A-D5 | V1 report field set wording conflict | `category?:` optional on client receive-side; booleans/array required | Supervisor packet lists category optional vs parent §4.13.1 listing; integrated server validator always emits it; receive-side looseness safe | chat-wire.ts | tsc/build | none | none | SPEC-05 UI reads defensively | accepted |
| S4-A-D6 | Report-frame identifier presence | Identifiers optional on `diagnostic:report`; strict `string\|null` only on unavailable frame | Frozen null-echo convention enforced exactly where specified; available frame provably always carries route IDs from a valid request roundtrip | chat-wire.ts | tsc/build | none | none | advisory A1: Slice B may re-freeze required when handler lands | accepted |
| S4-A-D7 | Family field requirement choices unspecified | tool_result.toolName optional fallback; subagent quartet+eventType required; saved.turnId required; exchange_metadata.turnId optional | Mirrors SPEC-02 §9 carried-field sets + parent correlation rules | chat-wire.ts | parity diff vs §9 table | none | none | Slice B can tighten during gating | accepted_no_downstream_impact |
| S4-A-D8 | reason typing unspecified in packet | WsTurnEndMessage.reason narrowed to `'complete'\|'interrupted'\|'error'` | New-shape precision matching server terminal vocabulary; broad bag keeps loose reason for non-routed types | chat-wire.ts | reviewer server-bytes check | none | none | none | accepted |
| S4-A-D9 | Packet preferred discriminated union over bag where practical | Bag NOT converted; dedicated Ws* REQUIRED-shape interfaces + unions exported | Strict conversion would drag Slice-B-owned handler edits into Slice A; packet authorizes minimum for routed consumer | websocket.ts, chat-wire.ts | build+tsc | None today | Low (bag permissive until B adopts unions) | B dispatcher consumes ChatInFlightWireMessage | accepted |

Slice B (builder-proposed D-B1..B6; orchestrator-final classifications confirmed by acceptance reviewer):

| ID | Original contract | Actual change | Reason | Files | Checks | Observable effect | Risk | Downstream impact | Classification |
|---|---|---|---|---|---|---|---|---|---|
| S4-B-D1 | websocket.ts expected as Slice-A output only | Bag += optional streamSeq/diagnosticId + literal `availability?: 'unavailable'` | Mechanical gate reads + suppression marker without widening frozen routed REQUIRED shapes | types/websocket.ts | eslint/tsc/build/smoke | none on wire; type surface only | none | Slice C/D consume same fields | accepted |
| S4-B-D2 | Old begin snapshotted unfinalized previous turn into history | Safety net removed; strict §4.6 null/same/different rules | Parent-approved begin semantics forbid silent replacement of a live client turn; old net also ran GLOBAL helper resets violating §4.12 isolation | turn-lifecycle.ts | smoke a5–a9 + reviewer C2 proof | begin never rewrites a live turn or foreign namespace | reveal-lag supersede window (R1; server ordering makes legit arrivals impossible) | Slice C restore paths absorb reconnect cases | accepted |
| S4-B-D3 | status_update previously projected usage from ANY thread | Projection applies only when addressed thread === selected thread | Parent §4.12 closing sentence: visible context/status selected by thread, not overwritten by background threads | stream-handlers.ts | smoke g1–g3 + reviewer behavioral proof | Background turns cannot overwrite the visible meter | none intended | none | accepted |
| S4-B-D4 | redactMessageForLog was module-private | Exported as §5.5 suppression seam (production-path execution preserved) | Mandated end-to-end suppression-provable seam for C6/D suppression proofs; pure reducer on real dispatch path is not a product test hook | ws-client.ts | smoke d1/d2 + reviewer attestation | none | none | Slice D suppression proofs consume export | accepted |
| S4-B-D5 | Pending diagnostic wait lifecycle unspecified | 10 s bounded timeout + duplicate-in-flight diagnosticId rejection resolving null | Mechanical necessity; deterministic and value-free | chat-diagnostic-handlers.ts | smoke module exercise | bounded waits resolve null | none | SPEC-05 UI UX polish optional | accepted |
| S4-B-D6 | Null-echo correlation rules when echo lost unspecified | Ladder: id echo → turnId echo (same-turn waits) → timeout | Deterministic defense-in-depth compliant with frozen null-echo convention | chat-diagnostic-handlers.ts | reviewer cross-check vs server frames | denial resolution for echo-lost classes | branch unreachable for legit waits | none | accepted |

Slice-B advisories carried forward: **A1** namespace re-creation window between turn_end release and async finalize (HEAD-equivalent exposure; watched in Slice C install/drain triggers — coherence invariant argued at stream-handlers.ts advanceBeforeBody docblock); **A2** empty-text advancement inconsistency — CLOSED by Slice C binding repair item (advancement moved out of payload conditionals; pinned by smoke case 14). Plus builder-reviewer A2 (`peekFrontierState` export-for-proof — must not mature into fixture-only accessor), A5 (speculative fixes forbidden).

Slice C (builder-proposed S4-C-D1..D6 + orchestrator F-C1 repair record; final classifications after acceptance cycle):

| ID | Original contract | Actual change | Reason | Files | Checks | Observable effect | Risk | Downstream impact | Classification |
|---|---|---|---|---|---|---|---|---|---|
| S4-C-D1 | SPEC §4 advisory surface | Two NEW files `live-route.ts` (208L) + `snapshot-restore.ts` (400L) as focused homes | Dispatcher stays thin single-job; roadmap §5.7 new-file ≤400L bound holds | live-route.ts, snapshot-restore.ts | eslint/tsc/build; bounds table | none | none | Slice D drives observable surfaces these expose | accepted |
| S4-C-D2 | Export surface unspecified | Public exports: resolveLiveRoute/LiveRoute/cascadeBufferedApplication/claimInstalledSnapshotBaseline/rememberAcceptedSnapshot/recallAcceptedSnapshot/readInstalledSnapshotBaseline/InstalledSnapshotRecord (+liveUserRowId private) | Mandated seam wiring between routing and restore engine; no second whole-turn counter introduced | ws modules | tsc/build | none | none | Slice D/SPEC-05 consume documented seams only | accepted |
| S4-C-D3 | Line-bound dispositions expected | stream-handlers 418→415; registry 330L (mandated ledger/seam, single job); chatSlice +19 (absent-key winner-semantics docs AND `applySavedExchangePayload` function→exported — amended wording after reviewer note) | §5.7 sentence 2 allows documented pre-existing growth under focused edits | stream-handlers/registry/chatSlice | wc -l vs table | none | none | recorded for parent-criterion-22 audit | accepted |
| S4-C-D4 | Parent §4.9.1 snapshot restoration presentation | In-flight restoration yields catch-up instant row + separate post-N tail row; saved ack merges tail | Boundary cosmetics explicitly deferred to RCC-0112 by parent §4.9.1; chronology/content proven correct | snapshot-restore.ts | smoke cases 1–3,9,11 | Visible boundary split of restored turns (cosmetic territory) | none — correctness invariant intact | RCC-0112 cosmetic follow-up; SPEC-05 aware | downstream_impact |
| S4-C-D5 | Monotone baseline rule | Sanctioned open-race rebuild exception: refused claim on wiped/unrepresented slot restores newest accepted projection from registry cache | Prevents real projection loss from duplicated thread:open frames given handler wipe ordering; refuses whenever own-turn representation exists | snapshot-restore.ts, stream-helper-registry.ts | FC1 probe+control (88/88 smoke); acceptance re-review | Duplicated open frames no longer strand wiped slots | closed F-C1 realization defect during gate cycle (see repair record) | Slice D may reuse probe shape for race case | accepted |
| S4-C-D6 | SPEC §5 test list realization module | Case-15 negative asserted at resolveLiveRoute module boundary in throwaway harness (message paths intrinsically wipe slots = server-authoritative replacement); positive paths via real handlers | Evidence-strength choice; dispatcher calls the same production gate entry | throwaway harness (non-repo) | builder+reviewer corroboration | none (evidence artifact only) | none | Slice D covers negatives through the browser fixture publicly | advisory (accepted) |

**F-C1 repair record (orchestrator-directed intra-slice repair, analogous to SPEC-02 F1/F2 pattern):** acceptance review #9 returned FINDINGS — slotRepresentsTurn's user leg matched durable foreign exchanges by prompt content, so a duplicated equal thread:open could refuse rebuild and strand a wiped slot with no live-turn projection (criterion C2 violation; realistic-duplication-race trigger; fully recoverable ⇒ material, not high). Repair: turn-deterministic detector legs via new single-source `liveUserRowId(turnId)` helper (both installer ensure-blocks share it; content matching forbidden on refusal/rebuild paths); trimmed prose kept file ≤400L. Regression pair added (FC1 probe reproduces reviewer scenario incl. cache-rebuild proof via stale-echo absence; control pins equal-N idempotence fingerprint before==after). Full smoke 88/0. Fresh reviewer #10 CLEAN first pass on repaired bytes. Classification: `repair_required` → resolved within slice; no cross-slice invalidation (only snapshot-restore.ts bytes changed; five sibling hashes unchanged; all other gates' evidence stands).

Acceptance-reviewer advisories carried forward: **A1** make report-frame identifiers required in Slice B handler work; **A2** `category` required consideration same time; **A3** snapshot regression guard (older-snapshot-cannot-regress) is Slice C dispatch logic — acceptFrontierBaseline installs any N unconditionally today; **A4** buffer duplicate-seq rejection belongs to turn/route gate (Slice B); drain stays exactly-once regardless; **A5** diagnostic log suppression currently satisfied-by-comment — end-to-end ownership lands Slice B (redactMessageForLog).

Slice D (builder-proposed S4-D-D1..D10; orchestrator S4-D-D11/D12 from residue remediation; classifications by orchestrator, adjudicated against reviewer #12 report):

| ID | Original contract | Actual change | Reason | Files | Checks | Observable effect | Risk | Downstream impact | Classification |
|---|---|---|---|---|---|---|---|---|---|
| S4-D-D1 | SPEC §4 surface lists spec+fixture only | Throwaway run-1 probe `e2e/__wa_probe.spec.ts` deleted and NOT recreated | Probe was picked up by default Playwright discovery and always threw ⇒ permanently broke any full-suite Playwright run incl. SPEC-05 global gate; throwaway non-repo artifact per S4-C-D6 lineage | (deleted file) | `--list` discovery + lane runs post-deletion | Full-suite discovery clean for SPEC-05 | none | removes a permanent future-gate hazard | accepted |
| S4-D-D2 | Advisory file list has fixture only | Two additional helper modules `e2e/support/working-activity-wire.ts` + `-scenario.ts` | §4 latitude: "Fixture location may follow an existing e2e helper convention" — one-job split keeps each ≤400L (287/214) | support/wire.ts, support/scenario.ts | eslint + gate lane | none (test infra) | none | SPEC-05 reuses same helpers in same file/convention | accepted |
| S4-D-D3 | Fixture "no product-visible hooks"; hermetic scenarios | View-state neutrality shim inside fixture: canned `state:get` → empty result, echoed `state:set` without server round-trip | Pre-shim boots wrote runtime view state into owner ai/** via real-server persistence and later boots hydrated poisoned persisted state ⇒ lane-wide store→DOM freeze (reproduced pre-fix incl. legacy specs); shim restores determinism AND stops owner-tree writes | ws-fixture.ts | containment test (mtime flips, SHA identical ⇒ no value flow) | hermetic lanes; zero runtime-state writes during tests | masks persisted-view-state regressions from THIS suite | advisory C: SPEC-05 packet must know suite proves state-neutral behavior | accepted |
| S4-D-D4 | Scenario settle semantics unspecified | Per-thread `openHandled(threadId,count)` handled-open barriers replace absolute `settled(2·target+1)` arithmetic | Absolute counting coupled to incidental boot chatter (observed hang served=4<min=5); per-thread counting is deterministic | scenario.ts, ws-fixture.ts | full green lane ×4 independent runs | race-free thread-switch sync | none | SPEC-05 inherits barrier API | accepted |
| S4-D-D5 | Frozen test drafted with `tokenUsage:{context_pct}` frames | status_update scenario traffic uses top-level `contextUsage` fraction | Server truth: translator emits no context_pct on this stream; client projection input is the top-level fraction (S4-B-D3 era). Product NOT edited — frames made server-truthful instead | spec.ts, wire.ts | meter title 10%→87% through public UI on real packaged dispatch | same public assertion power, truthful wire shape | none | none | accepted |
| S4-D-D6 | Frozen draft asserted `.streaming` live plane at restore/refusal | Assertions corrected to claim exactly §5 guarantees (baseline already-revealed ⇒ NO live plane until post-N events) | Over-reaching assertions asserted presentation SPEC-05 owns; §5 says already-revealed baseline only | spec.ts L198/L280 | green lane | assertions scoped to owned proof | none | presentation claims remain SPEC-05's | accepted |
| S4-D-D7 | Frozen A/B isolation case kept unpersisted B live plane across reopen | Redesigned around canonical-mirror reopen: B's turn mirrored into authoritative reopened snapshot (seq 3, FLUID-B-ONE/‑TWO) | Unpersisted live plane cannot survive reopen — slot wipe = server-authoritative replacement (banked semantics); prior case contradicted the accepted engine | spec.ts L324–343 | green lane; ordering+counts prove non-interference | isolation still proven cross-thread w/o contradicting C2 semantics | none | none | accepted |
| S4-D-D8 | wire factory emitted `subagentEventType:'start'` | `'TurnBegin'` production vocabulary | Production emits subagent intros on 'TurnBegin'/'ToolCall'; 'start' was dead vocabulary so factory never exercised the real path | wire.ts | `(sa-b)` intro visible from chunked args | factory matches live vocabulary | none | SPEC-05 subagent UI cases reuse factory | accepted |
| S4-D-D9 | Gate/check mechanism implied tsconfig coverage | Repo tsconfigs exclude e2e/ ⇒ ESLint over e2e glob + Playwright load-time compile/exec are THE repo check mechanism for these files | Mechanical fact of this worktree; documented per instruction to record implementation-equivalent owning mechanism | tsconfig.json/app/node (read-only), eslint.config.js | GATE-1 list exit 0; lane compiles files | none | type-drift caught at lint/load not tsc | note for SPEC-05 | accepted |
| S4-D-D10 | Roadmap §5.7 new-file ≤400L bound | working-activity.spec.ts pinned at EXACTLY 400L through all repairs via in-file compensation (comment consolidation, T_B alias inline, inflightSnapshotBase extension absorbing three inline boot specs) | Bound discipline under zero-headroom constraint | spec.ts | wc -l after every repair | none | zero headroom persists for SPEC-05 additions — must compensate in-file likewise | bound-carry note for SPEC-05 | accepted |
| S4-D-D11 | Fixture must never mutate anything outside owned bytes | Run-1-era pre-shim boots DID write runtime view state into owner ai/** (System/state + Views/002 tab metadata, mtime 2026-08-27T01:36) | Found by acceptance-advisory A investigation; forward-prevented by D-D3 (empirically proven: mtime moves, SHA stable) | ai/RC-MacAir-15/System/state/state.json (+Views/002 residual churn left as-is) | git diff vs HEAD; residue rg sweep | tracked System/state restored byte-identical to HEAD (diff EMPTY); Views/002 cosmetic tab-timestamp churn left untouched (no fixture markers; mixed attribution — clobbering risk outweighs cleanup value) | if real app had run concurrently 01:36, some churn could be owner-authored (attribution uncertain) | supervisor informed at SPEC review; no downstream SPEC impact | accepted (repair executed) |
| S4-D-D12 | Same as D-D11 | Untracked runtime `ai/RCs-Air-2/System/state/state.json` (hostname-default machine identity when FUSION_LOCAL_MACHINE unset) carried stale residue `currentThreadId:"wa-thread-a"` rewritten every boot | Neutralized to `{"currentThreadId":null,"workspace":{}}` so subsequent boot flushes persist neutral content; file had no recoverable baseline (untracked+gitignored) | ai/RCs-Air-2/System/state/state.json | post-edit sweep `rg wa-thread` clean; next-boot SHA watch available | phantom thread reference gone before any real app reads it | minimal (value was already fixture-authored) | none | accepted (repair executed) |

Slice-D advisories carried: **D-A'** byte-exact console-format coupling (`util.inspect` shape) is loud-fail maintenance cost only; **D-C** D-D3 masking residual — this suite proves state-neutral paths and will NOT catch persisted-view-state regressions (must inform SPEC-05 packet); plus SPEC-05 carries the 400L zero-headroom compensation convention (D-D10) and the openHandled/settled barrier API (D-D4).

Carried-advisory status updates after Slice D acceptance: **B-A5 / C-A5 CLOSED** — end-to-end client-side suppression of retrieved diagnostic fields is now proven through the packaged client's own console path by the browser complement (byte-exact allowlist, raw fields absent); **C-A1/A2 remain open informational advisories** (receive-side looseness for report-frame identifiers/`category` classified safe at S4-A-D5/D6; SPEC-05 UI reads defensively; no owner ruling required); **C-A3** covered behaviorally by frontier regression-refusal case + C's own smoke pair; **C-A4** duplicate-seq handling unchanged from accepted B bytes (drain stays exactly-once regardless).

## 7. Handoff facts for SPEC-05 (FINAL — integration closed 2026-08-27, run 3; final-integration reviewer #14 confirmed adequacy)

- **Typed activity/terminal message delivery contract:** routed REQUIRED shapes in `src/types/chat-wire.ts` (`ChatInFlightWireMessage` union — all ten in-flight members require positive-integer `streamSeq`), `WsTurnEndMessage.terminalError?` (PRESENT only on genuine persistence success, NEVER null, identical `TurnTerminalError` across wire/persisted/snapshot/merge), `LiveTurnSnapshot.terminalError` non-optional-null (snapshots always carry, null-init); usage mirror nullable-tolerant — NEVER read usage from terminal snapshots (S2-D9). SPEC-05 owns strictly-greater activityRevision presentation proof and stale-Working-resurrection proof (revision is mirror-only in client bytes today — no compare/order/suppress anywhere, verified by reviewer #14 grep).
- **Retained terminal-envelope state location:** the completed-message row (`id === turnId`) in the panel chat slot: `snapshot-restore.ts` `installCompletedInstantRow` retains the `terminalError` envelope onto that row ONLY when durable history lacks the matching exchange (absent-key winner semantics); `chatSlice.ts` `applySavedExchangePayload` merges save acks without touching identity/content/segments. SPEC-05 proves exactly-one-visible-error from this retained envelope through the public error UI.
- **Deterministic fixture API + accepted tags:** `installWaFixture(context, initialReplies) → WaFixture` with ops `{ push, setReplies, resetReplies, sentFrames, whenOpen, settled, openHandled(threadId,count) }` (e2e/support/working-activity-ws-fixture.ts, 349L; routeWebSocket proxy, no product-visible hooks; view-state neutrality shim cans `state:get`/`state:set` — see S4-D-D3 masking residual). Scenario helpers in support/working-activity-scenario.ts (214L): `startWaSession/selectThread/pushFrames/setOpenReply/clearOpenReplies/assistantRows/transcriptText/rowOrder/expectStaysAbsent/streamingCount/expectTranscript/ConsoleTap`. Server-shaped frame factories in support/working-activity-wire.ts (287L; types imported from real production modules). Accepted tags in `e2e/working-activity.spec.ts` (400L): `@routing` + `@frontier` ONLY — SPEC-05 extends THE SAME FILE with `@working`/`@terminal-error`; the file sits EXACTLY at the 400L bound ⇒ additions require in-file compensation (D-D10 convention); every Playwright command explicit `--workers=1`; per-thread `openHandled` barriers are the settle API. Optional hardening SPEC-05 may take: one-line negative guard asserting no `chat-turn:diagnostic:get` in `sentFrames()` after error-row mount (acceptance reviewer #13 advisory 1).
- **Carry-forward line-boundary audit dispositions for parent criterion 22:** inherited from SPEC-03: wire-broadcaster.test.js 411L (D-F1), prompt-canonical-route.integration.test.js 546L (DEV-3), thread-runtime-controller.js 498L. SPEC-04 additions: `snapshot-restore.ts` 400L (NEW at bound, accepted S4-C-D1/D3 + F-C1 repair), `stream-handlers.ts` 415L (baseline growth under focused edits, S4-C-D3), `ws-client.ts` 412L (baseline; suppression seam export S4-B-D4), `stream-helper-registry.ts` 330L (mandated seam ledger), `chatSlice.ts` 367L (+19 documented S4-C-D3), `working-activity.spec.ts` 400L (at bound, D-D10), plus NEW bounded files ws-fixture 349L / wire 287L / scenario 214L / tool-stream-handlers 252L / chat-wire 258L / chat-diagnostic-handlers 196L / frontier 145L; Slice A split modules websocket 187L / workspace 177L / view-state 233L / chat 296L / index hub 22L; turn-lifecycle 229L / subagent-stream 180L / tool-grouper 227L / thread-handlers 269L / live-route 208L. All new/extracted ≤400L; `LiveSegmentRenderer.tsx` untouched (DO-NOT-SPLIT held).

**SPEC-05 must also consume (from deviations/advisories):** D-D3 masking residual (suite proves state-neutral paths; persisted-view-state regressions invisible to it); receive-side typing tolerance for report-frame identifiers/`category` (runtime validation strict; UI reads defensively — C-A1/A2 informational); B-A1 namespace re-creation window (HEAD-equivalent, coherence invariant documented at stream-handlers advanceBeforeBody); pre-existing unrelated lane failures `chat-context-meter.spec.ts` / `typing-effect.spec.ts` predate SPEC-04 and persist without its bytes (owners outside RCC-0108 §7 table for those proofs — flag to supervisor for disposition at SPEC-05's global Playwright gate).
