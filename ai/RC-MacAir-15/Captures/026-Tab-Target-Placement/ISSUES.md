# TABS-03 Issue Ledger

**Updated:** 2026-09-04

| ID | Issue | Authority | Affected artifact | Dependency impact | Status | Resolution / future gate |
|---|---|---|---|---|---|---|
| TTP-I01 | TABS-02A implementation required explicit owner acceptance. | TABS-02A report | TABS-03 baseline | Former implementation prerequisite | `resolved` | RC accepted the exact reviewed TABS-02A report/baseline on 2026-09-04; commit and publication remain separate. |
| TTP-I02 | Existing tab data, shell presentation, and reservations are exposed through related but separate active-code structures; placement cannot mutate only one projection. | Active code + state standards | Slices 1–2 | Requires an atomic connected-owner seam | `validated` | SPEC B09/B14 require one transition/application boundary without a second store. |
| TTP-I03 | A simple first-match search would make duplicate exact targets nondeterministic. | Exact-match contract | Slice 1 | Acceptance gap if undefined | `validated` | Fail closed as `ambiguous_existing_target`; prove no mutation/reveal. |
| TTP-I04 | Direct fill can race with an Empty launcher reservation. | Accepted TABS-01 lifecycle | Slices 1–2 | Risks overwriting pending work | `validated` | Any reservation on the active Empty tab makes it unavailable; append instead. |
| TTP-I05 | Resolving before exact-target matching would make already-open content unavailable whenever creation-time resolution fails. | Exact-target priority contract | Slices 1–2 | Adds an unnecessary availability dependency | `validated` | Match/activate first; resolve exactly once only when fill or append is required. |
| TTP-I06 | The production Capture/File adapters do not yet use the generic component placement model. | Active code | TABS-03 scope | None for this package | `deferred` | One production view per later `VIEW-*` SPEC. |
| TTP-I07 | Placement needs to become a provenance-aware action chokepoint, but TABS-03 has no event, actor, timestamp, or ledger authority. | Shared coordination contract | BRIDGE-01 | Blocks provenance integration, not placement | `deferred` | BRIDGE-01 wraps the accepted controller/result boundary after TABS-03. |
| TTP-I08 | Durable tab restoration and exact-target dedupe after restart are not yet designed. | TABS-02A non-goals | Later worksurface persistence | None for in-memory acceptance | `deferred` | A dedicated persistence/hydration SPEC must define schema and migration. |
| TTP-I09 | Dynamic registrations and user-controlled permissions cannot safely supply arbitrary placement descriptors yet. | Owner control-plane direction | PROV-02 / control plane | None for first-party code-owned resolver | `deferred` | DB-authoritative registration and permission package. |
| TTP-I10 | TABS-02A's report status and shared coordination row had drifted from “ready for orchestration” to “implementation ready for owner review.” | TABS-02A report | Shared ledger/handoff | Could permit premature TABS-03 dispatch | `validated` | Coordination records are corrected with this bundle. |
| TTP-I11 | Two sources can request the same unopened target before either observes the other's state commit. | Exact-target dedupe + state standards | Slice 2 | Could create duplicate committed tabs | `validated` | Serialize placement decision/commit per connected host against latest state; release the lane before awaiting presenter reveal. |
| TTP-I12 | The first draft's match pseudocode referenced `resolved.presenterId/targetKey` before target resolution. | TTP-D09 | SPEC §7.2 | Inexecutable variable ordering | `validated` | Match existing tabs against validated `request.target` and resolve only after zero matches. |
| TTP-I13 | Whole-snapshot rejection conflicted with TABS-02A's preservation of chrome around invalid/unavailable component bodies. | TABS-02A D5/B25 | SPEC §§6.3, 7.1–7.3 | Could block unrelated navigation or destroy recovery state | `validated` | Validate collection identity/ownership envelopes; preserve and skip content/presentation-invalid, legacy, or unaddressed tabs as placement-unavailable protected records. |
| TTP-I14 | “If the owner can detect” commit failure did not define acknowledgement or keep the concurrency lane through asynchronous commit. | State standards + TTP-D15 | SPEC §§6.3, 7.5–7.6 | Could return false success or duplicate from stale state | `validated` | Exact atomic commit acknowledgement now holds the lane until the committed snapshot is returned and validated. |
| TTP-I15 | `ViewTabDescriptor` has no active runtime validator, so resolver display bounds were previously implicit. | Validation standards + active code | SPEC §6.2 | Independent implementations could accept hostile/oversize display fields | `validated` | Exact fields, data-property rules, byte bounds, and inert URL treatment are specified. |
| TTP-I16 | `view-tab-runtime.spec.ts` is a known non-hermetic failure in this checkout due to absent Capture fixtures and dirty persisted File state. | TABS-02A report | SPEC §11 | An unconditional zero-exit gate was infeasible and out of scope to repair | `validated` | Use the accepted deterministic Capture/File matrix as the gate; record the runtime test diagnostically unless a hermetic fixture is available. |
| TTP-I17 | Failure `requestId` was undefined when the ID itself was valid but another request field was malformed. | Correlation contract | SPEC §6.4 | Different implementations would echo or discard correlation | `validated` | Safely echo only an independently valid own enumerable data property; otherwise `null`; add exact cases. |
| TTP-I18 | The first reviewed request contract did not assign `requestId` an exact byte bound. | Validation/testability standards | SPEC §6.1 | A 257-byte ID had two conforming interpretations | `validated` | `requestId` and `presenterId` now use the exact 256-byte ID bound; `targetKey` uses 512 bytes. |
| TTP-I19 | The first reviewed pass criteria required every listed regression command to exit zero while separately permitting the known `view-tab-runtime` diagnostic result. | TABS-02A evidence limitation | SPEC §11 | Contradictory acceptance gate | `validated` | Zero-exit applies to blocking commands; the diagnostic has its own exact expected-outcome rule. |
| TTP-I20 | The refreshed candidate conflated a structurally valid exact target whose current renderer/body is unavailable with a structurally invalid placement record. | Exact-target priority; TABS-01 stable descriptor identity; TABS-02A degraded rendering | SPEC §§7.2, 8, 10 | Could either activate the valid target or skip it and create a duplicate | `validated` | Rendering availability is not identity: match the valid addressed descriptor/shell first; only structurally invalid, legacy, or unaddressed records are skipped and protected. |
| TTP-I21 | The active-Empty rule called any malformed active record `invalid_state`, conflicting with the protected nested-invalid record policy. | TTP-D16; TTP-I13 | SPEC §§7.2–7.3, 10 | Could either append or reject the same valid collection with protected nested invalidity | `validated` | Only malformed outer collection/active-identity ownership rejects; a uniquely identified active record with protected nested component/presentation invalidity cannot be filled and causes `current` to append. |

No release-blocking product question remains in TABS-03. TTP-I01 is resolved;
candidate review and owner approval remain release gates.
