# Agent Tool Provenance Implementation Ledger

**Bundle state:** `ROADMAP_COMPLETE`  
**Candidate ID:** `AGENT-TOOL-PROV-616b34ab8f748fd2`  
**Aggregate SHA-256:** `616b34ab8f748fd247a2ff3cb31f6bfa5d1c3483a48a43f845e07db1751d36cb`  
**Target worktree:** `/Users/rccurtrightjr./projects/fs-dev-wt-universal-view-tab-bar`  
**Review gates:** final authority `CLEAN`; final implementation readiness `READY / CLEAN`  
**Owner approval:** 2026-09-03  
**Current SPEC:** SPEC-01 owner-accepted; roadmap complete

| SPEC | Approval | Implementation | Owner acceptance |
|---|---|---|---|
| SPEC-01 Agent Tool Activity and Resource Checkpoints | approved as `AGENT-TOOL-PROV-616b34ab8f748fd2` on 2026-09-03 | complete; integrated review `CLEAN` | accepted 2026-09-04 |

## Supervisor Rules

- Dispatch only the exact approved candidate recorded by `RELEASE-MANIFEST.md`.
- Verify the accepted predecessor baseline before dispatch.
- Use one fresh SPEC orchestrator for the approved SPEC and the slice/review lifecycle in `GUIDANCE.md`.
- Record implementation reports and exact changed-path evidence without rewriting the approved intent.
- Present the integrated result to the owner; only explicit owner acceptance completes this SPEC.

## Standalone Orchestrator Run

The owner directed this orchestration to complete without a Roadmap Implementation Supervisor above it and to return the completed packet for manual review handoff. This changes only the handoff recipient/status vocabulary; it does not change the approved product contract or slice/review lifecycle.

### Preflight Evidence

- Candidate authority: 26/26 ordered paths matched; aggregate `616b34ab8f748fd247a2ff3cb31f6bfa5d1c3483a48a43f845e07db1751d36cb`.
- Accepted predecessor: 66/66 product paths matched; aggregate `15c18e67df1d9a69ef96fea6819ade662de23b1f41234c824eb2e92351237590`; deleted `fusion-studio-client/src/hooks/useFileTree.ts` remains absent.
- Baseline revision: `806b33521907ed4b51cf792d5ef664fe319ea349` on `main`; accepted predecessor/product and unrelated owner/runtime changes are uncommitted and must be preserved.
- Code Standards supersessions: none. ATP-D15 through ATP-D17 are the only approved older-Wiki overlays.

### Slice Ledger

| Slice | Scope | Prerequisite | Required gate | Builder | Acceptance reviewer | Current revision | Deviations/downstream impact | State |
|---|---|---|---|---|---|---|---|---|
| 01a | migration 036; validators/repositories; activity/edge/checkpoint/bind/observation/projection persistence primitives; query repository; shared path coordinator | accepted predecessor | builder review + orchestrator inspection + fresh acceptance review | `/root/slice_01a`; builder gate 16 `CLEAN` | `/root/slice_01a_orchestrator_review_02` `CLEAN` | HEAD `806b335`; 20-path digest `f612d147ecb4fa710928f222fdf7011b997120d92cf1a7c503dbd2b86eec7383` | SQLite non-rowid-alias bind PK; cumulative test-fixture integration; in-scope save coordinator extension; no temporary trigger remains | accepted |
| 01b | canonical timing/capture; OpenCode extraction; immutable turn authority; bounded reservation/finalization | accepted 01a | builder review + orchestrator inspection + fresh acceptance review | `/root/slice_01b`; builder gate 13 `CLEAN` | `/root/slice_01b_orchestrator_review_05` `CLEAN` | HEAD `806b335`; 23-path digest `b4a184285791fc3dfc17055a013b9ffbe9bf9a2369bef43b63c524f2e960665e` | synchronous 01a repository overlap; dedicated local `busy_timeout=0` connection; drain-local AsyncLocalStorage application contexts; global shutdown registration remains 01c | accepted |
| 01c | final fact authority; admission; two-fact ledger; shutdown graph | accepted 01b | builder review + orchestrator inspection + fresh acceptance review | `/root/slice_01c`; builder gate 14 `CLEAN` | `/root/slice_01c_orchestrator_review_03` `CLEAN` | HEAD `806b335`; 39-path digest `7767339c6e8d5bf65397d6553f9aa5717dbd240f1f93560124f3c22ad1d586b0` | accepted 01b shutdown overlaps; announced recovery; dedicated reconciliation DB; retained required-ack handler ownership; global deadline repairs | accepted |
| 01d | secure native observer; checkpoints; observation fact; v2 server projection; schedulers | accepted 01c | builder review + orchestrator inspection + fresh acceptance review | `/root/slice_01d` + `/root/slice_01d_repair_01`; replacement gate 03 `CLEAN` | `/root/slice_01d_orchestrator_review_01` `CLEAN` | HEAD `806b335`; 35-path digest `3700a5e868aa586f1b00f866c19631ff2df1c1bd2a19c158ac0a9901408d1746` | native/package integration; accepted earlier-slice overlaps; Darwin-only ONNX packaging exclusions; non-Darwin follow-up | accepted |
| 01e | activity query protocol/route; exchange binding integration | accepted 01d | builder review + orchestrator inspection + fresh acceptance review | `/root/slice_01e`; builder gate 05 `CLEAN` | `/root/slice_01e_orchestrator_review_02` `CLEAN` | HEAD `806b335`; 28-path digest `2359d01db23e2124970a31241873f77ee3b334503a388c65ace728d95357f177` | audit-save shutdown ownership; safe cursor symmetry; shared selector-log redaction; legacy no-authority compatibility | accepted |
| 01f | client v2 integration; guarded runtime proofs; integrated report | accepted 01e | builder review + orchestrator inspection + fresh acceptance review | `/root/slice_01f`; builder gate 02 `CLEAN` | `/root/slice_01f_orchestrator_review_01` `CLEAN` | HEAD `806b335`; 19-product-path digest `c82ab338862d3b54d9c93efe1f5a7111cdaad2c724df91560c377705e986e62e`; report SHA `6b1f201417be79ebe9005e546a91288f29828f6fcdfa538d510b25a561645dc6` | guarded fixture ingress; exact authority audit; v1 client overlaps; nonce redaction; report artifact | accepted |

Only one product-code writer may be active. Every new slice receives a fresh `spec-slice-builder`; every review pass receives a fresh read-only `clean-room-reviewer` under `spec-review-gate`. Findings repair forward until the first clean current-byte pass.

### Slice 01a Acceptance Record

- Builder lifecycle: gates 01–03 and 05–11 repaired material findings; gate 04 was superseded; gate 12 was clean but superseded by orchestrator findings; gates 13 and 15 repaired material findings; gate 14 was superseded after an unauthorized extra-trigger remedy was rejected; gate 16 returned `CLEAN`.
- Orchestrator repair cycle: the first acceptance reviewer found BOM rejection and invalid `admitted/not_ready` source states. Both were repaired. The orchestrator additionally rejected two temporary attempt-3 triggers because the approved migration permits exactly one trigger; the final implementation enforces the cross-row invariant in repository transactions and migration tests assert exactly one trigger.
- Final independent acceptance: `/root/slice_01a_orchestrator_review_02` returned `CLEAN` on digest `f612d147ecb4fa710928f222fdf7011b997120d92cf1a7c503dbd2b86eec7383` after 89 targeted, 266 focused, and 1,137 passing full-server tests with one existing skip.
- Accepted implementation detail: `agent_exchange_bind_jobs.exchange_id` uses SQLite `INTEGER NOT NULL PRIMARY KEY DESC` so a missing external identity rejects instead of silently rowid-autoassigning; downstream code must address the explicit `exchange_id` and not assume `rowid` equality.
- Accepted bounded integration: cumulative migration/test helpers include migration 036, and the existing save mutex now exposes the approved non-queuing observation lease with one save-priority handoff.
- Review-process deviations only: gates 01–02 mislabeled repairable findings as `BLOCKED`; the orchestrator treated them as `REPAIR_REQUIRED`. No final product behavior depends on that vocabulary error.

### Slice 01b Acceptance Record

- Builder lifecycle: gates 01, 03, 04, and 07–09 repaired material identity, fail-open, reservation, parsing, lifecycle, replay, and rebound defects; gates 02, 05, 06, and 11 were stale/superseded; gates 10 and 12 were clean on later-superseded bytes; gate 13 returned `CLEAN` on the accepted candidate.
- Orchestrator repair cycle: acceptance review 03 found same-WebSocket foreground drains rereading mutable session authority and cross-applying concurrent A/B events. The repair introduced frozen-authority-bound, drain-local application contexts while retaining one exact lifecycle/finalizer per turn. Acceptance review 04 then found stale pending and rebound-stop state retained in the base socket session; the repair added compare-and-swap natural-completion cleanup, exact-authority Stop finalization, safe legacy cleanup, and live-authority resolver precedence.
- Final independent acceptance: `/root/slice_01b_orchestrator_review_05` returned `CLEAN` on digest `b4a184285791fc3dfc17055a013b9ffbe9bf9a2369bef43b63c524f2e960665e` after 225 focused tests with open-handle detection and 1,262 passing full-server tests with one existing skip.
- Accepted bounded integration: `activity-repository.js` is the sole Slice 01a overlap, providing the synchronous atomic terminal-reservation and exact replay/conflict port. A dedicated shared `better-sqlite3` connection enforces `busy_timeout=0` without changing global Knex policy and is owned by the activity owner for bounded cancellation, cleanup, and disposal.
- Accepted turn isolation: every foreground/headless drain retains frozen prompt authority and drain-local chat/tool state across awaits; concurrent same-socket turns, rebound Stop, iterator/process completion, high-churn tombstone eviction, late work, and duplicate/conflicting terminal snapshots preserve original ownership and exactly-one finalization.
- Downstream allocation: Slice 01c must register the supplied activity-owner shutdown primitive in the approved global eight-second shutdown phase graph before database close. No global startup/shutdown composition was added in Slice 01b.
- Review-process deviation only: gate 06 was invalidated because the orchestrator used overlapping `sed` ranges and mistook their repeated boundary line for duplicate source. The bytes were unchanged and a fresh gate was used; no product behavior depends on the artifact.

### Slice 01c Acceptance Record

- Builder lifecycle: gates 01–10 and 13 repaired material admission, scheduling, retry, reconciliation, SQLite-lock, active-turn, shutdown, and handler-lifetime defects; gates 02 and 05 were superseded by builder self-review; gates 11 and 12 were clean on later-superseded bytes; gate 14 returned `CLEAN` on the accepted candidate.
- Orchestrator repair cycle: acceptance review 01 found swallowed TERM/KILL stop failure; acceptance review 02 found an unreferenced sole outer timer that allowed natural status-0 exit after incomplete cleanup. Both were repaired. Builder gate 13 then found sequential Phase-A provenance drains and detached required-ack handler continuations; the final repair runs post-finalizer owners concurrently and retains underlying handlers through controller stop.
- Final independent acceptance: `/root/slice_01c_orchestrator_review_03` returned `CLEAN` on digest `7767339c6e8d5bf65397d6553f9aa5717dbd240f1f93560124f3c22ad1d586b0` after 209 focused tests and 1,332 passing full-server tests with one existing skip, all with open-handle detection where applicable.
- Accepted bounded integration: Slice 01b activity-owner, canonical applier, canonical bridge, and focused tests are overlapped only for global shutdown ownership and fencing. `announced-activity-reconciler.js` and the dedicated file-backed reconciliation Knex connection provide bounded crash recovery with `foreign_keys=ON`, `busy_timeout=0`, and Phase-A disposal.
- Accepted shutdown behavior: the outer 8,000-ms force timer stays referenced until successful cleanup clears it; incomplete Phase A or B reaches exit 1. Active-turn finalization is the sole causal prerequisite, after which activity, admission, and ledger owners drain concurrently under the common absolute deadline while announced recovery drains independently. Reconciliation SQLite closes only after every owner succeeds.
- Accepted controller behavior: required-ack delivery still reports `timed_out` at the exact 2,000-ms deadline, but the underlying handler remains owned until actual settlement. Controller stop repeatedly sweeps dynamic in-flight work, observes late rejection, and prevents post-close SQLite access or normal exit while a handler remains active. `test/subscriptions/controller.test.js` is the approved 39th path proving this invariant.
- Downstream allocation: Slice 01d must register observation and renderer-projection owners in the same concurrent absolute-deadline graph. No native observer, observer subscription, v2 projection, activity query route, binder runtime, or client behavior was activated in Slice 01c.

### Slice 01d Acceptance Record

- Builder lifecycle: the original `/root/slice_01d` repaired builder gates 02–04, then exhausted its usage allocation during gate 05 before a terminal handoff. The preserved current work transferred to fresh `/root/slice_01d_repair_01`; replacement gates 01–02 repaired capability, shutdown, disposition-cap, and coordinator-deadline defects, and replacement gate 03 returned `CLEAN` on the accepted bytes.
- Final independent acceptance: `/root/slice_01d_orchestrator_review_01` returned `CLEAN` on digest `3700a5e868aa586f1b00f866c19631ff2df1c1bd2a19c158ac0a9901408d1746` after 111 focused tests and 1,379 passing full-server tests with one existing skip, plus native rebuild, client build, Electron directory packaging, and actual packaged `spawnServer` observer smoke.
- Accepted observer/security behavior: the observer receives exact host capabilities rather than ambient filesystem or raw database access; native C traversal is descriptor-relative with full root/parent identity revalidation, monotonic cancellation/deadline checks, one owned byte buffer, and secure zero/free. Missing, replaced, or unresolvable roots close as `workspace_unavailable` without candidate-byte reads.
- Accepted scheduling behavior: observation and projection delayed/recovery work share the normative 16/100 disposition budgets; pre-socket work remains capped and continuations are post-listen; shutdown fences late root resolution, native results, database transitions, and projection callbacks. Observation preserves four-global/one-per-activity ownership and save-priority serialization.
- Accepted deadline behavior: the same-path lease releases at the absolute two-second coordinator deadline even if an injected non-cooperative native promise remains scheduler-owned. Cancellation fires, late bytes are zeroed, and the result is permanently barred from persistence or projection; this preserves the SPEC's explicit bounded save wait while retaining truthful cleanup ownership.
- Accepted projection/checkpoint integration: real `resource.state_observed@1` reservations use the final Slice 01c admission and ledger path; committed durable jobs govern strict `resource:changed@2` delivery/fallback; first, changed, unchanged, absence, skip, failure, retry, crash, and lease recovery remain source-row authoritative.
- Accepted deviations: earlier-slice overlaps in checkpoint/job repositories, save coordinator, registry/compiler, startup/server, and package wiring are mechanically required. macOS packaging excludes Linux/Windows ONNX payloads; verified Darwin arm64 behavior is unchanged, while any future non-Darwin package must revisit the exclusion. Directory packaging is intentionally unsigned and retains existing icon/metadata warnings.
- Operational evidence: two early package attempts reached resource copy but failed for insufficient disk. Only regenerable npm caches (about 0.8 GiB) and exact failed `release` outputs were cleared; no repository source, user profile, workspace, or database was removed. Subsequent clean packages and packaged observer smoke passed.
- Inventory reconstruction: the original builder's exact handoff was lost with its usage exhaustion. Mtime evidence and accepted-slice inventories reconstructed the same 35 owned paths; unchanged accepted-01a `test/agent-provenance/jobs-and-checkpoints.test.js` was inspected/rerun but excluded from 01d ownership.
- Downstream allocation: Slice 01e may add only the activity query protocol/route and exchange binding integration. Slice 01f retains client v2 rendering and end-to-end live UI proof. Runtime/native acceptance evidence is Darwin arm64, Node ABI 141.

### Slice 01e Acceptance Record

- Builder lifecycle: gate 01 was interrupted before changed-byte review after builder self-review found missing verification; gate 02 repaired legacy harness exchange rollback; gate 03 was clean on bytes later superseded by orchestrator findings; gate 04 repaired inbound selector logging; gate 05 returned `CLEAN` on the accepted candidate.
- Orchestrator repair cycle: acceptance review 01 found asynchronous audit exchange saves were not shutdown-owned and result `nextCursor` accepted values its own query rejected. The repair added explicit audit task ownership with active-turn → audit-save → binder causality under the shared absolute Phase-A deadline, plus symmetric positive-safe-integer cursor validation. Builder gate 04 additionally added shared redaction for inbound activity `path`, `folderPrefix`, and `fileName` while preserving the routed message.
- Final independent acceptance: `/root/slice_01e_orchestrator_review_02` returned `CLEAN` on digest `2359d01db23e2124970a31241873f77ee3b334503a388c65ace728d95357f177` after 163 focused tests and 1,429 passing full-server tests with one existing skip and open-handle detection.
- Accepted query behavior: strict `agent:activity@1` query/result/error validation, selector normalization, workspace/epoch isolation, root-folder semantics, combined filters, safe descending pagination, bounded summaries, and safe exchange detail references are installed through the public router and feature advertisement. Responses and diagnostics exclude raw arguments/results, snapshot bytes, absolute roots, and rejected outside paths.
- Accepted binding behavior: exchange insertion and bind-job creation are one transaction using the row's `exchangeTs`; metadata and binder clocks remain distinct. The concurrency-one binder preserves 100-item/one-second startup bounds, post-commit wakes, finite retry/suppression, restart recovery, conflicts, direct/cascading delete behavior, and bounded shutdown. No historical backfill occurs.
- Accepted shutdown behavior: active-turn finalizers synchronously start their `turn_end` audit tasks; audit saves drain before the binder can stop; unrelated owners remain concurrent under the same absolute Phase-A deadline. Audit timeout or failure blocks later database close and normal exit, leaving no post-close SQL callback.
- Accepted compatibility behavior: exchanges without complete provenance authority remain operational but intentionally create no bind job. The optional fifth immutable `HistoryFile.addExchange` authority argument and startup-installed writer/signal preserve legacy callers.
- Accepted deviations: earlier-slice registry, router, startup, shutdown, and test overlaps are mechanically required. Shared redaction-map integration satisfies the WebSocket privacy boundary. The source-boundary redaction test exceeds the advisory expected-file list but directly verifies a required criterion. Test execution regenerated only accepted native build output.
- Downstream allocation: Slice 01f may add the client v2 protocol/store/File Viewer integration, guarded live fixture, full runtime proof, and implementation report only. Client UI remains unchanged by Slice 01e.

### Slice 01f Acceptance Record

- Builder lifecycle: gate 01 returned `REPAIR_REQUIRED` after reproducing fixture-authorization nonce disclosure in inbound WebSocket logging; the shared redaction map was extended without mutating routed input. Gate 02 returned `CLEAN` on the accepted current bytes.
- Final independent acceptance: `/root/slice_01f_orchestrator_review_01` returned `CLEAN` on product digest `c82ab338862d3b54d9c93efe1f5a7111cdaad2c724df91560c377705e986e62e` and report SHA `6b1f201417be79ebe9005e546a91288f29828f6fcdfa538d510b25a561645dc6` after independently rerunning every mandatory command and the mediated-save live regression.
- Accepted client behavior: the strict client `resource:changed@2` union mirrors the immutable server schema; version-directed handling preserves v1, rejects malformed/unknown v2, enforces workspace/epoch isolation, deduplicates exact dominant-edge projection replay, closes on conflicting stable identity, and uses the existing central store for targeted content/tree invalidation and refetch. No new panel or Office/Email dirty-buffer mutation was added.
- Accepted live evidence: the marker/nonce-owned deterministic OpenCode-shaped fixture uses dynamic non-3001 ports, a closed catalog, guarded temporary profile/workspaces/output, disabled/audited watchers and child processes, same-database restart, exact locked-authority comparison, and protected-state hashes. It proves edit/read/error/interruption, bytes/absent interleavings, A → mediated B → agent-observed A, snapshot reuse, timestamp preservation, query/binding, projection dedupe, and no watcher delivery.
- Required command evidence: full server 138 suites/1,432 pass plus one existing skip; source Playwright 58/58; client production build; Electron directory package; packaged native observer through actual `spawnServer`; guarded agent live; and guarded mediated-save live all passed on accepted bytes.
- Accepted deviations: guarded fixture ingress and exact isolated authority audit are bounded production-inert test integration; predecessor client-file overlaps are mechanically necessary; the implementation report is a workflow artifact; nonce redaction is a required shared-boundary security repair. The fixture remains as regression infrastructure until an equally isolated adapter-boundary harness fixture replaces it.
- Residual platform impact: runtime/native evidence is Darwin arm64, Node ABI 141. The accepted non-Darwin packaging follow-up remains unchanged. The interruption fixture narrowly seeds in-flight activity because production OpenCode capture is terminal-snapshot-owned; the real shared finalizer and all downstream owners process the interruption.

### Final SPEC Integration Record

- Final reviewer: `/root/spec01_final_integration_review_01` returned terminal `CLEAN` and classified the integrated SPEC ready for owner review, explicitly not owner-accepted.
- Authentication: HEAD `806b33521907ed4b51cf792d5ef664fe319ea349`; approved 26-path aggregate `616b34ab8f748fd247a2ff3cb31f6bfa5d1c3483a48a43f845e07db1751d36cb`; predecessor aggregate `15c18e67df1d9a69ef96fea6819ade662de23b1f41234c824eb2e92351237590`; accepted slice digests 01a–01f match their ledger rows. The final implementation report SHA is `a91c246c9559de21188d264502dd01a6e5b91f1cc768d4f22aa9265fa9085520` after documentation-only terminal reconciliation.
- Required integrated checks: full server 138/138 suites with 1,432 passed and one existing skip; source Playwright 58/58; client production build; Electron Darwin arm64 directory package; packaged native observer through actual `spawnServer`; guarded agent live; and guarded mediated-save live all passed independently in the final gate.
- Integrated contract result: migration/persistence, immutable capture authority, two-fact admission/ledger, secure native observation, sparse checkpoints, durable renderer projection, activity query/exchange binding, client v2 invalidation/refetch, shutdown/recovery, redaction, compatibility, and guarded runtime isolation are materially clean. No required check was skipped and no unexplained cross-slice drift was found.
- Final deviation impact: accepted deviations are compatible and bounded. Future non-Darwin packaging must revisit Darwin package exclusions; the production-inert deterministic fixture remains regression infrastructure until an equally isolated adapter-boundary replacement exists. No owner ruling is required.
- Operational cleanup: no target-worktree process, tested-port listener, marker-owned temporary directory, or live-test handle remained. Protected profile/database/workspace hashes were unchanged. Earlier cleanup removed only regenerable npm caches and failed package output; user data was untouched.
- Owner receipt: the owner explicitly accepted the SPEC-01 Agent Tool Provenance implementation on 2026-09-04. The one-SPEC roadmap is complete. The final reviewer authenticated the pre-receipt implementation report as SHA-256 `a91c246c9559de21188d264502dd01a6e5b91f1cc768d4f22aa9265fa9085520`; the owner-receipt report is SHA-256 `21258a8b49fe7d545a09c8e56dd23f00cc9a4ed1a5f54ec7a5b78694da8f0a15`. The only report-byte change records this receipt.
- Final status: `ROADMAP_COMPLETE`.
