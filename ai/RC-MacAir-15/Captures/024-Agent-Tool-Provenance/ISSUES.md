# Agent Tool Provenance Issue Ledger

## Validated For This Candidate

| ID | Issue | Resolution | Authority | Status |
|---|---|---|---|---|
| ATP-I01 | Tool calls exist inside exchange JSON but are not granularly indexed | Normalize tool activities and resource edges; retain exchange reference for raw detail | ATP-D03/D04 | validated |
| ATP-I02 | Canonical harness timestamps are discarded by the bridge | Preserve reported time and add host receipt/snapshot clocks | ATP-D05 | validated |
| ATP-I03 | `exchangeId` does not exist until turn persistence and a post-commit callback has a crash gap | Insert a durable bind job inside the exchange transaction, drain it after commit/startup, and retain `chat-turn:saved` only as an optional scheduling signal | active code + ATP-D03 | validated |
| ATP-I04 | A post-tool snapshot cannot prove causation or guarantee a first preimage | Label it observation; chain only to prior observed checkpoint | ATP-D02/D11 | validated |
| ATP-I05 | Raw tool arguments may duplicate secrets and whole-file content | Store hashes and safe extracted metadata only | ATP-D04 | validated |
| ATP-I06 | Broad Bash interpretation creates false confidence and attack surface | Non-executing, literal-only, capped parser with durable extraction basis | ATP-D07 | validated |
| ATP-I07 | Existing governed registry accepts only `resource.mutated@1` | Extend closed schema/filter/capability/handler catalogs for exact new built-ins | ATP-D09 | validated |
| ATP-I08 | Existing `resource:changed@1` semantically requires create/modify | Add observation-specific v2 projection; retain v1 unchanged | ATP-D13 | validated |
| ATP-I09 | Repeated unchanged reads/writes could flood snapshots | Sparse snapshot rows plus content-addressed blobs; activity/edge rows remain factual | ATP-D10/D14 | validated |
| ATP-I10 | Harness events are applied synchronously and callers do not await them | Make canonical application awaitable and preserve per-turn order at every caller | active code constraint | validated |
| ATP-I11 | Failed or interrupted tools may partially mutate before reporting terminal state | Observe candidates for completed, error, result-phase bounce, and interrupted records; a verified pre-execution bounce remains explicitly unexecuted | ATP-D02/D08 | validated |
| ATP-I12 | Existing Wiki decisions leave broad canonical tool lifecycle, identity, args/result, and hash policy blocked | ATP-D15 authorizes only this candidate's bounded observational activity/fact identity and local fingerprints; broad `chat.tool.*`, accepted-ref, native-ref, captured-output, and automation branches remain open | Tool Call Provenance Schema + ATP-D15 | validated |
| ATP-I13 | Existing Wiki decisions reserve snapshot/blob persistence and its full-snapshot-versus-diff choice for later file versioning unless explicitly folded into current work | ATP-D16 narrowly answers ULV-D02 with eligible full exact snapshots/no diffs and folds in only the bounded agent-observation checkpoint/blob store plus `resource.state_observed@1`; it leaves general `file.version`, diff, restore, retention, and broader ULV branches open | Events/Ledger/File Versioning decisions + ATP-D16 | validated |
| ATP-I14 | Mutable renderer/session workspace can change during a live turn | Capture one private server-owned turn authority at prompt acceptance and pass it through every provenance stage | Architecture/WebSocket Standards | validated |
| ATP-I15 | Stop, late terminal events, and nonsettling processes can race or hang | One ordinal FIFO turn finalizer with exact terminal precedence and fixed TERM/KILL/drain/shutdown deadlines | Harness/Persistence Standards | validated |
| ATP-I16 | Prose-only table bullets permit invalid lifecycle/outcome rows | Exact column, null/default, FK/delete, CHECK/semantic, and transaction matrices govern migration 036 | Persistence Standard | validated |
| ATP-I17 | Rebuilding a tool fact after observation/binding changes its body | Persist immutable fact JSON/hash in the terminal reservation and replay only that body | UEB Standard | validated |
| ATP-I18 | Post-exchange binder has a crash gap and delete can split ID/save/bind times | Insert a bind job in the exchange transaction; startup drain plus triple-clearing delete trigger | Persistence Standard | validated |
| ATP-I19 | Path `lstat` plus later open permits symlink/parent-swap races | Bundled native descriptor-relative root/parent traversal and final `openat`/`fstatat` checks; no JavaScript pathname fallback | Architecture/Persistence Standards | validated |
| ATP-I20 | Existing reserved-path behavior conflicts with absent→bytes | Absent checkpoints carry no live identity; tombstone old live mapping and mint normal successor only for bytes | accepted 023 resource identity | validated |
| ATP-I21 | First-read/unchanged-write ordering can suppress refresh | Every successful eligible observation projects; same-activity path groups use write-dominant single observation while retaining access edges | ATP-D11/D13 | validated |
| ATP-I22 | A command-name list is not an executable shell parser | Closed tokenizer, operators, per-command options/cardinality/access table, ambiguity rejection, dedupe and fixtures | ATP-D07 | validated |
| ATP-I23 | OpenCode start/end/envelope times were being collapsed | Map `state.time.start`, `state.time.end`, and envelope `timestamp` to distinct reported fields | ATP-D05 + active OpenCode evidence | validated |
| ATP-I24 | A terminal tool snapshot could be mistaken for a turn-terminal event | Make it terminal only for that activity; only true turn boundaries close ingress | active OpenCode evidence + Harness Standard | validated |
| ATP-I25 | Terminal-derived Settings arguments could take a false pre-execution bounce and hash a different result than the exchange | Resolve the existing result-phase bounce once before reservation, hash the final JSON-safe exchange value, and expand without re-evaluation | ATP-D08 + active code | validated |
| ATP-I26 | OpenCode completed/error status could be derived inconsistently from state, exit metadata, or chat display state | Map only exact `part.state.status` to provenance status and keep chat normalization separate | ATP-D15 + active OpenCode evidence | validated |
| ATP-I27 | Invalid shell input requested `truncated=true` without detecting a 65th candidate | Use `0/0/false` plus a fixed diagnostic; reserve truncation for actual candidate overflow | exact relational contract | validated |
| ATP-I28 | Startup reconciliation could not verify the prompt-captured root after losing in-memory turn authority | Persist a private canonical-root digest plus paired device/inode identity on the activity and revalidate it before recovery reads | Architecture/Persistence Standards | validated |
| ATP-I29 | Missing/symlink/non-regular terminal lookups did not pin and revalidate parent identity | Apply stable authorized-parent verification and one retry to every terminal lookup outcome before recording or retiring identity | path-containment contract | validated |
| ATP-I30 | An ATP-unchanged state can differ from the current File Viewer store after an intervening mediated save | Keep sparse checkpoint/fact storage but project every successful eligible edge observation, using the dominant edge UUID for idempotency | ATP-D13 + live-render outcome | validated |
| ATP-I31 | Parent validation before a decisive lookup still permits a swap during that lookup | Revalidate captured root and pinned parent again immediately after every decisive absent/skip/bytes lookup and before accepting the outcome | path-containment contract | validated |
| ATP-I32 | Terminal Settings enforcement could use mutable session root after workspace navigation | Evaluate exactly once using only the immutable prompt-captured root and combine protected-symlink with A→B→A fixtures | ATP-D08 + immutable authority | validated |
| ATP-I33 | Timed-out UEB handlers and unbounded backlog could detach reads, exhaust buffers, or consume save queue capacity | Durable singleton scheduler with exact concurrency/claim/retry/startup/shutdown bounds plus save-priority non-queuing observer acquisition | bounded provenance + mediated-save preservation | validated |
| ATP-I34 | Pre-reservation Settings enforcement emission could precede creation of its tool part | Purely evaluate/build the final value before reservation; emit only in ordered result expansion after call/args | ATP-D08 + existing chat order | validated |
| ATP-I35 | Interrupted turns could be forced to await asynchronous observation retries beyond finalization deadlines | Durably close/reserve/signal, then end the turn; scheduler owns all later filesystem attempts | bounded finalization | validated |
| ATP-I36 | Scheduler/projection paths could act before their governing facts were admitted | Claim jobs only for admitted tool facts; gate first/changed v2 on admitted checkpoint fact and otherwise send identity-free refresh-required | ATP-D09 + UEB authority | validated |
| ATP-I37 | Omitted over-cap raw-value fingerprints made exact raw duplicate comparison impossible | Define equality over the bounded persisted normalized phase and explicitly collapse unobservable raw-only differences | progressive disclosure + bounded hashing | validated |
| ATP-I38 | LED-D04 still blocked the two new migration-029 projections | ATP-D17 narrowly authorizes exact tool/observation mappings, transactions, replay, retry bounds, and diagnostics only | Ledger authority + ATP-D17 | validated |
| ATP-I39 | Candidate fingerprints had no canonical or bounded input | Domain-separate accepted canonical UTF-8 paths from bounded rejected UTF-16LE prefixes and define dedupe/cap ordering | deterministic bounded extraction | validated |
| ATP-I40 | Fresh Fusion receipt clocks could make an exact provider replay falsely conflict | Treat host IDs/receipt/bookkeeping clocks as first-write-owned and compare only producer-controlled normalized fields | timestamp truth + idempotency | validated |
| ATP-I41 | Checkpoint-admission recovery lacked an exact strict envelope | Reuse v1 with admitted activity ID and fixed reason mapping; never expose pending checkpoint IDs | UEB/WebSocket authority | validated |
| ATP-I42 | Exchange save time could mean metadata `savedAt` or row `exchanges.ts` | Capture one `exchangeTs` inside insertion transaction, use it for row/job, and capture binder time independently | ATP-D05 | validated |
| ATP-I43 | A partial staged tool part could receive a false exact detail reference | Mark only atomically completed terminal expansion and require marker plus available hash agreement before binding | ATP-D04 + progressive detail truth | validated |
| ATP-I44 | A fresh ledger projection clock could make exact duplicate replay conflict | Treat `event_log.created_at` as first-write-owned, exclude it from replay comparison, and preserve it on both fact replays | ATP-D05/D17 | validated |
| ATP-I45 | Repeated SQLite contention had no terminal bound and could retry forever | Persist three bounded projection cycles with +250/+1,000 ms eligibility and terminal `failed`; no fourth automatic cycle | bounded recovery + ATP-D17 | validated |
| ATP-I46 | A multi-path observation job had ambiguous attempt and due-time ownership | Store attempts per sibling path group, derive job due time from pending edges, choose only ready groups, and define lock/lease behavior | bounded observation scheduler | validated |
| ATP-I47 | Tool-summary resource aggregates and canonical fact edge order were underdefined | Use contiguous retained ordinals, fact-array ordinal order, and distinct canonical-path counts with exact changed semantics | deterministic query/fact contract | validated |
| ATP-I48 | `resource:changed@2.occurredAt` could be reconstructed from the wrong clock | Reuse the persisted dominant edge `observed_at` for initial send and every retry | ATP-D05/D13 | validated |
| ATP-I49 | A path-based parent recheck can miss a swap-and-restore race | Pin and traverse descriptors in the required native addon; fail closed when secure primitives are unavailable | Architecture/path-containment contract | validated |
| ATP-I50 | Blob-table prose implied an impossible surrogate integer ID beside a SHA-256 primary key | State each table's exact primary key and explicitly give blobs no surrogate ID | Persistence Standard | validated |
| ATP-I51 | Pre-observation rejected candidates had no executable durable edge shape | Close them atomically as attempt-zero skipped edges at terminal-observed time and never schedule them | exact relational contract | validated |
| ATP-I52 | Parent traversal and final-open failures could receive inconsistent outcomes or unsafe fallback | Freeze a descriptor-relative phase/errno matrix with exact retry, absent, skip, failure, and no-fallback behavior | path-containment contract | validated |
| ATP-I53 | Required subscribers could run while the producer row still said fact admission was pending | Add a private exact-pair pre-dispatch commit after validation and before delivery; delivery failure never revokes admission | UEB admission ordering | validated |
| ATP-I54 | A lone observation claim could expire without any future scheduler signal | Make the sole timer cover both pending due times and running lease expiries, including startup arming | durable scheduler recovery | validated |
| ATP-I55 | Post-cycle bookkeeping could crash and permit unlimited uncharged ledger retries | Claim and durably charge every ledger cycle first; lease recovery consumes the charge and attempt three terminates | bounded ledger recovery | validated |
| ATP-I56 | Native packaged smoke targeted Electron run-as-Node while production spawns resolved system Node | Exercise the actual packaged `spawnServer` child path and copied addon in acceptance | active packaging/runtime path | validated |
| ATP-I57 | Observation attempt reservation did not identify the claimed path or distinguish pre-I/O crashes | Persist dominant claimed edge plus claimed attempt; exact lease recovery uses `observation_incomplete` without extra I/O | durable observation truth | validated |
| ATP-I58 | Pre-observation and observation-time `invalid_path` branches require different path-retention shapes | Null path metadata only for pre-observation rejection; retain accepted canonical metadata for a later parent-path invalidation | exact edge-shape contract | validated |
| ATP-I59 | Repeated transition-write contention could turn both schedulers into one-second polling loops | Permit one delayed bounded transition retry, then suppress that source/token until restart with no further timer | bounded recovery | validated |
| ATP-I60 | Observation transition failure did not define a truthful durable outcome | Discard any uncommitted filesystem result, run only no-result settlement, and otherwise retain/suppress the durable running claim for restart | persistence truth | validated |
| ATP-I61 | A delayed post-I/O database retry could either hold the path lock too long or commit stale bytes after releasing it | Cap total coordinator ownership at 2,000 ms, discard every uncommitted result before release, and require a fresh locked observation later | save/observation serialization | validated |
| ATP-I62 | Non-retryable observation and ledger scheduler-transition failures lacked a finite branch | Immediately diagnose and suppress the exact tokenless-source or running-claim key until restart, with no timer | bounded recovery | validated |
| ATP-I63 | Lock misses did not consume the observation batch budget and could defeat yielding | Bound by installed-claim dispositions while reserving edge attempt counts only for committed filesystem attempts | scheduler backpressure | validated |
| ATP-I64 | Pending tool/checkpoint fact admission had no executable bounded replay owner | Add one ordered concurrency-one admission reconciler with startup/time caps, yielded continuation, finite retries, suppression, and shutdown ownership | UEB admission recovery | validated |
| ATP-I65 | Slice 01c required observation-fact admission behavior before its authority was owned | Move both final fact schemas/publishers/admission and the final two-fact ledger contract into 01c; 01d reuses them without locked-row mutation | dependency-ordered slices | validated |
| ATP-I66 | Exchange-bind database failure could hot-loop or strand work without a declared recovery boundary | Use five immediate attempts, one delayed retry, then exact-ID suppression until restart; preserve pending truth | exchange binding | validated |
| ATP-I67 | Root-level `folder_path` had no canonical representation | Define exact empty string for workspace root and reject `.` or `/` aliases in persistence/query validation | canonical path/query contract | validated |
| ATP-I68 | Prose named an impossible ASCII ordering with `tool` before `observation` | Replace it with the explicit cross-table rank `tool=0`, `observation=1` in both admission and ledger order | deterministic reconciliation | validated |
| ATP-I69 | An active observer could reduce simultaneous save admission from 33 to 32 despite not consuming a waiter entry | Add one save-only priority handoff slot during observation and prove identical 32/33/34-save admission/FIFO behavior | mediated-save regression contract | validated |
| ATP-I70 | Crash windows could lose File Viewer invalidation or force an unbounded historical edge rescan | Create a projection job atomically with every successful dominant edge and drain it through stable-ID bounded claim/lease recovery | renderer projection recovery | validated |
| ATP-I71 | Path admission referred to mutable active workspace and undefined filesystem resolution for missing/symlink paths | Use an exact filesystem-free component algorithm against immutable prompt-captured root; leave state/type decisions to the native observer | immutable authority/path containment | validated |
| ATP-I72 | A mid-turn terminal reservation could block legacy chat indefinitely or mutate provenance after a timeout | Add a two-second cancellation gate, five immediate zero-busy-wait attempts, tracked late cleanup, and fixed fail-open continuation | chat continuity | validated |
| ATP-I73 | Separate shutdown allowances exceeded the active 2.5-second outer force timer and observation timers were not cancelled | Replace it with one concurrent-owner 8-second phase graph and cancel all timers before subscription/database close | lifecycle safety | validated |
| ATP-I74 | Final locked subscription definitions crossed slice boundaries and could reference inactive schemas or require checksum mutation | Install both fact schemas/final ledger contract in 01c, final v2 server authority/observer subscription in 01d, and leave 01f client-only | immutable seed/compiler contract | validated |
| ATP-I75 | Migration summary still said six tables after durable renderer projection added a seventh | Correct the normative migration count to seven, matching the exact table matrix | persistence schema coherence | validated |

## Explicit Non-Blocking Deferrals

| ID | Deferred work | Why non-blocking now | Future gate | Status |
|---|---|---|---|---|
| ATP-DI01 | User/button/manual-save provenance | Agent-only boundary is explicit | UI principal/save-event SPEC | deferred |
| ATP-DI02 | Trigger, script, NPM task, worker, and automation self-reporting | These need registered identities and permissions | Script/trigger provenance SPEC | deferred |
| ATP-DI03 | Arbitrary external filesystem changes | Explicitly excluded from MVP | Owner-approved connector or observation policy | deferred |
| ATP-DI04 | Restore command and UI | Snapshots and chain references are sufficient foundation | Restore/retention SPEC | deferred |
| ATP-DI05 | Retention, pruning, redaction, export, and encryption policy | Requires owner policy across chat and snapshots | Data lifecycle SPEC | deferred |
| ATP-DI06 | Binary/large-file snapshots and diffs | Owner approved narrow UTF-8 class | Expanded file-version policy | deferred |
| ATP-DI07 | Resource extraction beyond this candidate's exact OpenCode tool/field mappings | The extension seam is inert beyond the approved mappings | Separately owner-approved provenance SPEC plus per-adapter/tool/field fixtures | deferred |
| ATP-DI08 | Raw argument/result detail API or database tool | Existing exchange is the detail source; index provides exact reference | Authorized database/query-tool SPEC | deferred |
| ATP-DI09 | Causal confidence, evidence verdicts, or inferred mutation authorship | Timestamped facts satisfy current goal | Separate causal-analysis product decision | deferred |
| ATP-DI10 | Tool rights, permissions, execution interception, and custom OpenCode hooks | Explicitly excluded | Permission/principal SPEC | deferred |
| ATP-DI11 | Full-text search over shell commands or tool output | Would expose sensitive/raw detail and duplicate exchange content | Authorized search/redaction SPEC | deferred |
| ATP-DI12 | Unified UI-save plus agent provenance query | This SPEC exposes agent activity without changing stable v1 save query | Unified provenance-query SPEC | deferred |

## Blocking Issues

None. Approval of the exact candidate adopts ATP-D01 through ATP-D17, including the deliberately narrow older-Wiki overlays in ATP-D15 through ATP-D17, and resolves the product choices required for implementation.
