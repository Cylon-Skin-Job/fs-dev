# Handoff — 2026-05-27

## Current State

### Completed
- **Renamed** `System Files` → `System Source Files` (folder, symlink, DB, manifest, git committed)
- **Created** `view-modules/` at project root with `wiki/wiki-viewer/`, `wiki/wiki-guide/`, `wiki/wiki-sidebar/`
- **Built iframe-based wiki-viewer** with folder-driven structure:
  - Content sections: `workspaces/`, `enforcement/`, `system/`
  - Connectors content migrated from system-viewer to `system/connectors/`
  - Stubs (skills, triggers, scripts, hooks) removed from system-viewer
  - Auto-discovery from `index.json` files per folder
  - Left panel: sections + articles
  - Right sidebar: `chrome_reader_mode` guide header + groups + articles
  - Detail panel: markdown via `marked`
  - Header shows article name only (no breadcrumbs)
  - Guide filename drives display name (e.g., `Connectors_Guide.md` → "Connectors Guide")
- **Converted React WikiExplorer → iframe WikiViewer** in `ContentArea.tsx`
- **Created reusable modules** in `view-modules/wiki/`:
  - `wiki-viewer.js` — core engine (content loading, rendering, history)
  - `wiki-guide.js` — left panel paradigm (toggles, guide link)

### Active Design Discussion
- **Browser View Spec** written to `docs/BROWSER_VIEW_SPEC.md`
- Unified browser/app container concept:
  - Normal mode: URL bar, nav buttons, bookmarks
  - Fullscreen mode: chrome hidden, iframe only
  - Config via view `index.json`: `url`, `homepage`, `fullscreen`, `chrome.*`
  - Security: block `javascript:`, `data:`, `file:`, `fusion-studio://`
  - Phase 1 = basic container. Phase 2 = tabs, bookmarks, injected API.

### Next Session
- Another agent will **build Browser View** from the spec
- I will help **orchestrate and approve** that work

### Key Decisions Made
1. **wiki-guide** = System Panel paradigm (left toggles + guide content)
2. **wiki-viewer** = reusable wiki rendering module
3. **wiki-sidebar** = future standalone sidebar work
4. **Browser view** replaces need for complex IPC bridge — users run their own Node backend
5. **Minimal API surface** for integration: read-only SQLite queries + Calendar/Email/Todo CRUD
6. **User backend lifecycle**: NOT killed on workspace switch, independent of view lifecycle
