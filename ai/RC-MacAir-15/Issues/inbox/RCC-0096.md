---
id: RCC-0096
title: 'Code standards sweep 2026-06-11 — new violations + stale wiki checklist'
assignee: rccurtrightjr
created: 2026-06-11T00:00:00.000Z
author: rccurtrightjr
state: open
priority: medium
---

Full file-size/one-job sweep of fusion-studio-server and fusion-studio-client against the Code Standards wiki page (`ai/<machine>/Wiki/005-Enforcement/001-Code_Standards/PAGE.md`). Context: SPEC-01g landed the same day (server.js now a 302-line glue file) and legacy root config.js was deleted. agents-viewer was excluded from the sweep (intentionally in flux).

---

## New Violations (not tracked by any spec)

| File | Lines | Finding |
|------|-------|---------|
| `fusion-studio-client/src/lib/ws/stream-handlers.ts` | 708 | **Worst offender.** `handleStreamMessage` is a single 365-line function; subagent-stream handling (~140 lines) is a separable domain. This file was the *result* of the SPEC-05a split and has regrown past the threshold. Turn lifecycle is fragile here (see spec 05 history). |
| `fusion-studio-server/lib/transcription/cleanup-list-pass.js` | 751 | Cohesive (all functions detect/format spoken lists) — the "350-line SSE controller" case, but at 2× that size. Review, not urgent. |
| `fusion-studio-server/lib/workspace/workspace-controller.js` | 500 | Two jobs: workspace lifecycle (validate/restore/switch/create) + ribbon management (4 handlers, ~150 lines). Ribbon extraction is the natural split. |
| `fusion-studio-server/lib/views/index.js` | 489 | Two jobs: read-side view discovery/resolution + write-side workspace registry mutation (add/restore/install templates). |
| `fusion-studio-client/src/types/index.ts` | 547 | Type barrel, likely a dumping ground. Low priority. |

## Post-Split Files Still Over 400 (watch list)

Mostly single-job routers/appliers, acceptable per the one-job rule, but worth watching:

- `lib/thread/thread-crud.js` — 507 (only one that smells re-splittable)
- `lib/file-explorer.js` — 500
- `lib/wire/canonical-chat-event-applier.js` — 485
- `lib/ws/client-message-router.js` — 465
- `lib/thread/ThreadManager.js` — 401 (right at the line)

## Known and Still Open

- `lib/harness/clis/qwen/index.js` (436) + `gemini/index.js` (408) — specs 10/14, the 95%-identical shared extraction.

## Already Cleared by Spec (no action)

- `base-cli-harness.js` (spec 09), `catalog-visual.ts` (spec 07), `LiveSegmentRenderer.tsx` (spec 13 — DO NOT SPLIT), `panelStore.ts` (spec 20). Test files treated as exempt.

## Stale Wiki Checklist

The Code Standards page's "Compliance Migration — Audit Specs" checklist does not reflect reality. Evidence says these specs are **done but unchecked**:

- SPEC-03 / SPEC-04 — `lib/thread/` now contains session-manager.js, thread-crud.js, thread-messages.js
- SPEC-05 — ws-client.ts is down to 239 lines (stream-handlers.ts is the extraction result)
- SPEC-06 — VoiceRecorder split into `src/mic/` (126-line component)
- SPEC-08 — HoverIconModal split into `components/hover-icon-modal/` parts
- SPEC-11 — compat.js is down to 289 lines
- SPEC-02 — RobinOverlay.tsx no longer exists under that name (verify rename before checking off)

(server.js / SPEC-01 was already checked off on 2026-06-11.)

---

## What Needs to Happen

1. **Update the wiki checklist** — verify and check off specs 02–06, 08, 11 on the Code Standards page.
2. **Spec + split stream-handlers.ts** — highest-value new finding; decompose `handleStreamMessage` and extract the subagent stream domain. Mind the documented turn-lifecycle fragility.
3. **Spec workspace-controller and views/index splits** — both are clean two-job separations (ribbon handlers out; registry mutation out).
4. **Decide on the borderline files** — cleanup-list-pass.js, types/index.ts, thread-crud.js: split, or rule "one job, acceptable" and record that on the wiki like specs 07/09/13.

## Verification

- Re-run the sweep: `find fusion-studio-server fusion-studio-client/src \( -name "*.js" -o -name "*.ts" -o -name "*.tsx" \) | grep -v node_modules | grep -v /dist/ | grep -v /release/ | xargs wc -l | awk '$1 > 400'`
- Every remaining >400 file is either spec-tracked or has a recorded "no action" ruling.
